# 🔑 Тестовые данные для входа

## ✅ Готовые тестовые аккаунты

Эти аккаунты уже созданы в базе данных и **НЕ требуют email верификации**.

### 👨‍🎓 Ученик 1
```
Email: student@test.com
Пароль: test123
Роль: Ученик
Класс: 9А
Баланс: 1000 ₽
```

### 👨‍🎓 Ученик 2
```
Email: student2@test.com
Пароль: test123
Роль: Ученик
Класс: 10Б
Баланс: 1500 ₽
```

### 👨‍🎓 Ученик 3
```
Email: student3@test.com
Пароль: test123
Роль: Ученик
Класс: 11В
Баланс: 2000 ₽
```

### 👨‍🍳 Повар
```
Email: chef@test.com
Пароль: test123
Роль: Повар
```

### 👨‍💼 Администратор
```
Email: admin@test.com
Пароль: test123
Роль: Администратор
```

## 🚀 Быстрый вход

### Для тестирования функций ученика:
1. Откройте http://localhost/student-auth
2. Введите: `student@test.com` / `test123`
3. Нажмите "Войти"

### Для тестирования функций повара:
1. Откройте http://localhost/chef-auth
2. Введите: `chef@test.com` / `test123`
3. Нажмите "Войти"

### Для тестирования функций администратора:
1. Откройте http://localhost/admin-auth
2. Введите: `admin@test.com` / `test123`
3. Нажмите "Войти"

## 📧 Проблема с email кодами

### Почему не приходят коды?

Email сервис настроен на отправку с адреса `ppredprof@gmail.com`, но для работы нужно:

1. **Включить 2FA в Gmail аккаунте**
2. **Создать пароль приложения**
3. **Обновить настройки в коде**

### Решение 1: Используйте тестовые аккаунты (рекомендуется)

Тестовые аккаунты уже верифицированы и не требуют email кодов!

### Решение 2: Настройте свой Gmail

#### Шаг 1: Включите 2FA
1. Перейдите: https://myaccount.google.com/security
2. Включите "Двухэтапная аутентификация"

#### Шаг 2: Создайте пароль приложения
1. Перейдите: https://myaccount.google.com/apppasswords
2. Выберите "Почта" и "Другое устройство"
3. Введите название: "School Canteen"
4. Скопируйте сгенерированный пароль (16 символов)

#### Шаг 3: Обновите настройки

Отредактируйте `server/utils/emailService.js`:

```javascript
const SMTP_CONFIG = {
  host: 'smtp.gmail.com',
  port: 587,
  secure: false,
  auth: {
    user: 'ваш-email@gmail.com',      // ← Ваш Gmail
    pass: 'xxxx xxxx xxxx xxxx'        // ← Пароль приложения
  }
}
```

#### Шаг 4: Перезапустите сервер

```bash
# Остановите
Ctrl+C

# Запустите заново
npm run server
```

### Решение 3: Используйте другой SMTP сервис

#### Mailtrap (для тестирования)
```javascript
const SMTP_CONFIG = {
  host: 'smtp.mailtrap.io',
  port: 2525,
  auth: {
    user: 'ваш-mailtrap-user',
    pass: 'ваш-mailtrap-pass'
  }
}
```

#### Yandex Mail
```javascript
const SMTP_CONFIG = {
  host: 'smtp.yandex.ru',
  port: 465,
  secure: true,
  auth: {
    user: 'ваш-email@yandex.ru',
    pass: 'ваш-пароль'
  }
}
```

#### Mail.ru
```javascript
const SMTP_CONFIG = {
  host: 'smtp.mail.ru',
  port: 465,
  secure: true,
  auth: {
    user: 'ваш-email@mail.ru',
    pass: 'ваш-пароль'
  }
}
```

## 🧪 Тестирование email

### Проверка подключения к SMTP

Добавьте в `server/index.js` после импортов:

```javascript
import { verifyEmailConnection } from './utils/emailService.js'

// После initDatabase()
verifyEmailConnection().then(result => {
  if (result) {
    console.log('✅ Email сервис работает')
  } else {
    console.log('❌ Email сервис не работает')
  }
})
```

### Тестовая отправка

Создайте файл `test-email.js`:

```javascript
import { sendVerificationCode } from './server/utils/emailService.js'

const testEmail = 'ваш-email@gmail.com'
const testCode = '123456'

sendVerificationCode(testEmail, testCode)
  .then(() => console.log('✅ Email отправлен!'))
  .catch(err => console.error('❌ Ошибка:', err))
```

Запустите:
```bash
node test-email.js
```

## 🔧 Альтернатива: Отключить email верификацию

Если email не критичен для тестирования, можно временно отключить верификацию.

### В файле регистрации (например, `server/routes/auth.js`):

Найдите код отправки email и закомментируйте:

```javascript
// Временно отключено для тестирования
// await sendVerificationCode(email, verificationCode)

// Автоматически верифицируем
await runQuery(`
  UPDATE users 
  SET email_verified = 1 
  WHERE email = ?
`, [email])
```

## 📝 Дополнительная информация

### Структура тестовых аккаунтов

Все тестовые аккаунты созданы с помощью скрипта `create-test-accounts.sql`:

```sql
-- Пароль: test123 (хешированный bcrypt)
-- Email верифицирован: 1 (да)
-- Баланс: от 1000 до 2000 ₽
```

### Применение тестовых аккаунтов

Если тестовые аккаунты не работают, примените SQL скрипт:

```bash
# Windows
sqlite3 school_canteen.db < create-test-accounts.sql

# Или используйте batch файл
apply-test-accounts.bat
```

### Проверка тестовых аккаунтов

```bash
# Откройте базу данных
sqlite3 school_canteen.db

# Проверьте тестовые аккаунты
SELECT email, role, email_verified, balance 
FROM users 
WHERE email LIKE '%@test.com';

# Выход
.exit
```

## 🎯 Рекомендации

### Для разработки:
✅ Используйте тестовые аккаунты (`*@test.com`)
✅ Не требуют email верификации
✅ Быстрый вход и тестирование

### Для production:
⚠️ Настройте реальный SMTP сервис
⚠️ Используйте переменные окружения для credentials
⚠️ Включите email верификацию
⚠️ Удалите тестовые аккаунты

## 🔒 Безопасность

**ВАЖНО:** Тестовые аккаунты предназначены только для разработки!

Перед production деплоем:
1. ❌ Удалите все тестовые аккаунты
2. ✅ Настройте реальный SMTP
3. ✅ Включите email верификацию
4. ✅ Используйте сильные пароли
5. ✅ Храните credentials в .env файле

## 📞 Поддержка

Если тестовые аккаунты не работают:
1. Проверьте что база данных инициализирована
2. Примените `create-test-accounts.sql`
3. Перезапустите сервер
4. Очистите кэш браузера

---

**Готово к тестированию!** 🎉

Используйте `student@test.com` / `test123` для быстрого входа.
