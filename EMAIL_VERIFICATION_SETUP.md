# 📧 Настройка Email-верификации

## ✅ Реализовано

Система отправки кодов верификации на email полностью интегрирована с использованием Gmail SMTP.

## 📋 Что было сделано

### 1. Создан модуль отправки email (`server/utils/emailService.js`)
- Использует `nodemailer` для отправки писем
- Настроен Gmail SMTP (smtp.gmail.com:587)
- Красивый HTML-шаблон письма с кодом
- Функция проверки подключения к SMTP серверу

### 2. Обновлен API (`server/routes/auth.js`)
- Интегрирована реальная отправка email
- Убрано возвращение кода в API ответе (безопасность)
- Добавлена обработка ошибок отправки email

### 3. Обновлен Frontend (`src/pages/StudentAuth.jsx`)
- Убрано отображение кода в alert
- Добавлено уведомление о проверке почты (включая папку "Спам")

## 🔧 Настройки SMTP

### Текущие настройки (из message.py):
```javascript
SMTP_CONFIG = {
  host: 'smtp.gmail.com',
  port: 587,
  secure: false,
  auth: {
    user: 'ppredprof@gmail.com',
    pass: 'xvzr khqt hckc wabb' // Пароль приложения Gmail
  }
}
```

### ⚠️ Важно для Gmail:
1. **Пароль приложения** (не обычный пароль аккаунта)
2. Включена **двухфакторная аутентификация**
3. Создан **пароль приложения** в настройках Google Account

## 📧 Шаблон письма

Письмо содержит:
- 🍽️ Заголовок "Школьная столовая"
- 6-значный код крупным шрифтом
- Информацию о сроке действия (10 минут)
- Предупреждение о безопасности
- Красивое HTML-оформление

## 🧪 Тестирование

### 1. Запустите приложение:
```bash
docker-compose up -d
```

### 2. Откройте браузер:
```
http://localhost:5000
```

### 3. Процесс регистрации:
1. Нажмите "Вход для ученика"
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Email: **ваш реальный email**
   - Пароль: test123
   - Имя: Иван
   - Фамилия: Иванов
   - Класс: 10А
4. Нажмите "Получить код"
5. **Проверьте почту** (включая папку "Спам")
6. Введите полученный 6-значный код
7. Нажмите "Зарегистрироваться"

### 4. Ожидаемый результат:
- ✅ Письмо приходит на указанный email
- ✅ Код действителен 10 минут
- ✅ Регистрация успешна с правильным кодом
- ❌ Ошибка с неправильным/истекшим кодом

## 🔍 Проверка логов

### Логи сервера:
```bash
docker logs school-canteen-app
```

### Успешная отправка:
```
✅ Verification code sent to user@example.com
Email sent successfully: <message-id>
```

### Ошибка отправки:
```
❌ Email sending failed: [error details]
```

## 🛠️ Устранение неполадок

### Проблема: Письмо не приходит

**Решение 1: Проверьте папку "Спам"**
- Gmail может помещать письма в спам

**Решение 2: Проверьте настройки Gmail**
```bash
# Проверьте логи
docker logs school-canteen-app | grep -i email
```

**Решение 3: Проверьте пароль приложения**
- Убедитесь, что используется пароль приложения, а не обычный пароль
- Пароль должен быть без пробелов: `xvzrkhqthckc wabb`

**Решение 4: Проверьте подключение к SMTP**
```javascript
// В server/utils/emailService.js есть функция:
verifyEmailConnection()
```

### Проблема: Ошибка "Invalid login"

**Причина:** Неверный пароль приложения или не включена 2FA

**Решение:**
1. Перейдите в Google Account → Security
2. Включите двухфакторную аутентификацию
3. Создайте новый пароль приложения
4. Обновите пароль в `server/utils/emailService.js`

### Проблема: Ошибка "Connection timeout"

**Причина:** Блокировка порта 587 или проблемы с сетью

**Решение:**
1. Проверьте firewall
2. Попробуйте порт 465 (secure: true)
3. Проверьте интернет-соединение

## 🔐 Безопасность

### ✅ Реализовано:
- Код не возвращается в API ответе
- Код не показывается в alert
- Код хранится с временем истечения (10 минут)
- Код удаляется после использования

### 🔒 Рекомендации для продакшена:
1. **Переменные окружения:**
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=ppredprof@gmail.com
   SMTP_PASS=xvzr khqt hckc wabb
   ```

2. **Rate limiting:**
   - Максимум 3 кода на email в час
   - Cooldown 60 секунд между запросами

3. **Redis для хранения кодов:**
   ```javascript
   // Вместо Map использовать Redis
   await redis.setex(`verification:${email}`, 600, code)
   ```

4. **Логирование:**
   - Записывать все попытки отправки
   - Мониторинг подозрительной активности

## 📊 Статистика

### Время отправки:
- Генерация кода: ~1ms
- Отправка email: ~1-3 секунды
- Общее время: ~3-5 секунд

### Лимиты Gmail:
- **500 писем/день** для бесплатного аккаунта
- **2000 писем/день** для Google Workspace

## 🚀 Альтернативные сервисы

Если нужна более надежная отправка:

### 1. SendGrid
```javascript
const sgMail = require('@sendgrid/mail')
sgMail.setApiKey(process.env.SENDGRID_API_KEY)
```

### 2. AWS SES
```javascript
const AWS = require('aws-sdk')
const ses = new AWS.SES({ region: 'us-east-1' })
```

### 3. Mailgun
```javascript
const mailgun = require('mailgun-js')
const mg = mailgun({ apiKey: API_KEY, domain: DOMAIN })
```

## 📝 Примеры кодов

### Успешная регистрация:
```
Email: test@example.com
Код: 123456
Результат: ✅ Регистрация успешна
```

### Истекший код:
```
Email: test@example.com
Код: 123456 (прошло > 10 минут)
Результат: ❌ Код истёк. Запросите новый код
```

### Неверный код:
```
Email: test@example.com
Код: 999999 (неправильный)
Результат: ❌ Неверный код подтверждения
```

## ✅ Готово к использованию!

Система email-верификации полностью настроена и готова к тестированию.

**Следующие шаги:**
1. Протестируйте с реальным email
2. Проверьте папку "Спам"
3. Убедитесь, что код приходит в течение 5 секунд
4. Проверьте истечение кода через 10 минут

---

**Дата:** 27 января 2026  
**Версия:** 2.0.0  
**Статус:** ✅ Готово к тестированию
