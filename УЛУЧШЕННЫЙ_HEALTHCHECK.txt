╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ HEALTHCHECK УЛУЧШЕН                                 ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ЧТО БЫЛО ИЗМЕНЕНО:                                      │
└───────────────────────────────────────────────────────────┘

PostgreSQL healthcheck:
✅ Увеличен интервал проверки: 5s → 10s
✅ Увеличено количество попыток: 5 → 10
✅ Добавлен start_period: 10s (время на запуск)
✅ Убраны переменные окружения из команды

Backend healthcheck:
✅ Увеличено количество попыток: 3 → 5
✅ Увеличен start_period: 40s → 60s

┌───────────────────────────────────────────────────────────┐
│  🚀 КОМАНДЫ ДЛЯ ЗАПУСКА:                                 │
└───────────────────────────────────────────────────────────┘

# 1. Остановите всё
docker-compose -f docker-compose.production.yml down

# 2. Удалите volume
docker volume rm bufet-software_postgres_data

# 3. Запустите PostgreSQL отдельно и следите за логами
docker-compose -f docker-compose.production.yml up postgres

# Если увидите "database system is ready to accept connections"
# Нажмите Ctrl+C и продолжите:

# 4. Запустите всё в фоне
docker-compose -f docker-compose.production.yml up -d

# 5. Следите за логами backend
docker logs school-canteen-backend -f

┌───────────────────────────────────────────────────────────┐
│  🔍 ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ POSTGRES:                    │
└───────────────────────────────────────────────────────────┘

PostgreSQL Database directory appears to contain a database; 
Skipping initialization

или

initdb: warning: enabling "trust" authentication for local connections
...
database system is ready to accept connections

┌───────────────────────────────────────────────────────────┐
│  ⚠️  ЕСЛИ POSTGRES НЕ ЗАПУСКАЕТСЯ:                       │
└───────────────────────────────────────────────────────────┘

ВАРИАНТ 1 - Проверьте порт:

netstat -tlnp | grep 5432

Если занят, измените в docker-compose.production.yml:

ports:
  - "5433:5432"

───────────────────────────────────────────────────────────

ВАРИАНТ 2 - Используйте другую версию PostgreSQL:

В docker-compose.production.yml измените:

image: postgres:15-alpine

на

image: postgres:14-alpine

или

image: postgres:16-alpine

───────────────────────────────────────────────────────────

ВАРИАНТ 3 - Запустите без healthcheck:

Закомментируйте healthcheck в docker-compose.production.yml:

# healthcheck:
#   test: ["CMD-SHELL", "pg_isready -U canteen_user -d school_canteen"]
#   interval: 5s
#   timeout: 5s
#   retries: 10
#   start_period: 10s

И измените depends_on в backend:

depends_on:
  - postgres

Вместо:

depends_on:
  postgres:
    condition: service_healthy

┌───────────────────────────────────────────────────────────┐
│  📊 ПРОВЕРКА:                                            │
└───────────────────────────────────────────────────────────┘

# Статус контейнеров
docker ps

# Логи PostgreSQL
docker logs school-canteen-db

# Логи Backend
docker logs school-canteen-backend

# Подключение к PostgreSQL
docker exec school-canteen-db psql -U canteen_user -d school_canteen -c "SELECT version();"

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🎯 ПОПРОБУЙТЕ ЗАПУСТИТЬ POSTGRES ОТДЕЛЬНО              ║
║      И ПРИШЛИТЕ ЛОГИ                                     ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
