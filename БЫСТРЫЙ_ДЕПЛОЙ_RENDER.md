# ⚡ БЫСТРЫЙ ДЕПЛОЙ НА RENDER

## За 10 минут

---

## 📋 ШАГ 1: Подготовка кода

### 1.1 Загрузите код на GitHub

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

---

## 🗄️ ШАГ 2: Создайте базу данных

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   ```
   Name: school-canteen-db
   Database: school_canteen
   User: canteen_user
   Region: Frankfurt (или ближайший)
   Plan: Free
   ```
4. Нажмите **"Create Database"**
5. **ВАЖНО:** Скопируйте и сохраните:
   - Internal Database URL
   - External Database URL

---

## 🌐 ШАГ 3: Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Заполните:
   ```
   Name: school-canteen-app
   Region: Frankfurt
   Branch: main
   Runtime: Node
   Build Command: npm install && npm run build
   Start Command: npm start
   Plan: Free
   ```

---

## 🔧 ШАГ 4: Настройте переменные окружения

В разделе **Environment Variables** нажмите **"Add Environment Variable"** и добавьте:

### Обязательные переменные:

```env
NODE_ENV=production
PORT=10000
```

### Переменные базы данных:

Скопируйте из Internal Database URL вашей PostgreSQL базы:

```
postgresql://canteen_user:PASSWORD@HOST:5432/school_canteen
```

Добавьте:
```env
DB_HOST=<HOST из URL>
DB_PORT=5432
DB_NAME=school_canteen
DB_USER=canteen_user
DB_PASSWORD=<PASSWORD из URL>
DATABASE_URL=<полный Internal Database URL>
```

### Секретный ключ сессии:

Сгенерируйте случайную строку:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Добавьте:
```env
SESSION_SECRET=<сгенерированная_строка>
```

---

## 🚀 ШАГ 5: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения сборки (3-5 минут)
3. Откройте URL вашего приложения

---

## 🔑 ШАГ 6: Примените тестовые аккаунты

### Вариант 1: Через Render Shell

1. Откройте ваш Web Service в Dashboard
2. Перейдите в **"Shell"** (вкладка сверху)
3. Выполните:

```bash
cat create-test-accounts.sql | psql $DATABASE_URL
```

### Вариант 2: Через локальный терминал

```bash
psql "ваш_EXTERNAL_DATABASE_URL" < create-test-accounts.sql
```

---

## ✅ ШАГ 7: Проверка

1. Откройте ваш URL: `https://school-canteen-app.onrender.com`
2. Нажмите "Вход для студентов"
3. Введите:
   ```
   Email: student@test.com
   Пароль: test123
   ```
4. Если вход успешен - **готово!** 🎉

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Ошибка подключения к БД:

1. Проверьте переменные окружения (DB_HOST, DB_PASSWORD)
2. Убедитесь, что база данных запущена (зеленый статус)
3. Проверьте Internal Database URL

### Приложение не запускается:

1. Откройте **Logs** в Dashboard
2. Найдите ошибку
3. Проверьте, что все переменные окружения заданы

### База данных пустая:

1. Приложение создаст таблицы автоматически при первом запуске
2. Если нет - примените тестовые аккаунты вручную (Шаг 6)

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

### Проверить статус:
```bash
# В Render Shell
psql $DATABASE_URL -c "SELECT COUNT(*) FROM users;"
```

### Посмотреть пользователей:
```bash
psql $DATABASE_URL -c "SELECT email, role FROM users;"
```

### Перезапустить приложение:
Dashboard → Manual Deploy → Deploy latest commit

---

## 🎯 ГОТОВО!

Ваше приложение теперь доступно по адресу:
```
https://school-canteen-app.onrender.com
```

Тестовые аккаунты:
- student@test.com / test123
- chef@test.com / test123
- admin@test.com / test123

---

## ⚠️ ВАЖНО: Free Plan

- Приложение засыпает после 15 минут неактивности
- Первый запрос после сна займет 30-60 секунд
- База данных удаляется через 90 дней неактивности

**Решение:** Используйте [UptimeRobot](https://uptimerobot.com) для пингов каждые 5 минут (бесплатно)

---

**Подробная инструкция:** RENDER_DEPLOYMENT.md
