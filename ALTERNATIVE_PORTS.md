# 🔌 Альтернативные порты и способы запуска

Если стандартные порты (3000 и 5000) не работают, используйте альтернативные варианты.

## 🚀 Быстрые команды

### Вариант 1: Стандартные порты (3000 и 5000)

```bash
# Терминал 1 - Сервер на порту 5000
npm run server

# Терминал 2 - Клиент на порту 3000
npm run dev
```

Откройте:
- http://localhost:3000
- http://127.0.0.1:3000
- http://0.0.0.0:3000

---

### Вариант 2: Альтернативный порт 3001

```bash
# Терминал 1 - Сервер на порту 5000
npm run server

# Терминал 2 - Клиент на порту 3001
npm run dev:3001
```

Откройте:
- http://localhost:3001
- http://127.0.0.1:3001

---

### Вариант 3: Порт 8080 (популярный альтернативный)

```bash
# Терминал 1 - Сервер на порту 5000
npm run server

# Терминал 2 - Клиент на порту 8080
npm run dev:8080
```

Откройте:
- http://localhost:8080
- http://127.0.0.1:8080

---

### Вариант 4: Порт 4000

```bash
# Терминал 1 - Сервер на порту 5000
npm run server

# Терминал 2 - Клиент на порту 4000
npm run dev:4000
```

Откройте:
- http://localhost:4000
- http://127.0.0.1:4000

---

### Вариант 5: Альтернативный порт сервера 5001

```bash
# Терминал 1 - Сервер на порту 5001
npm run server:5001

# Терминал 2 - Клиент на порту 3000
npm run dev
```

⚠️ **Важно:** Нужно обновить vite.config.js:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:5001', // Изменить на 5001
    changeOrigin: true
  }
}
```

---

### Вариант 6: Порт сервера 8000

```bash
# Терминал 1 - Сервер на порту 8000
npm run server:8000

# Терминал 2 - Клиент на порту 3000
npm run dev
```

⚠️ **Важно:** Нужно обновить vite.config.js:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8000', // Изменить на 8000
    changeOrigin: true
  }
}
```

---

## 🌐 Доступ по IP адресу

Если localhost не работает, используйте IP адрес:

### Локальный IP (127.0.0.1)
```
http://127.0.0.1:3000
http://127.0.0.1:5000/api/menu
```

### Сетевой IP

После запуска сервер покажет все доступные адреса:
```
🚀 Server running on:
   - Local:   http://localhost:5000
   - Network: http://127.0.0.1:5000
   - Network: http://192.168.1.100:5000  ← Ваш локальный IP
```

Используйте показанный IP адрес.

---

## 🔧 Ручная настройка портов

### Изменить порт клиента

**Способ 1: Через команду**
```bash
vite --port 9000
```

**Способ 2: В vite.config.js**
```javascript
export default defineConfig({
  server: {
    port: 9000  // Любой свободный порт
  }
})
```

### Изменить порт сервера

**Способ 1: Через переменную окружения**
```bash
PORT=6000 node server/index.js
```

**Способ 2: В server/index.js**
```javascript
const PORT = 6000  // Любой свободный порт
```

---

## 🐛 Проблемы и решения

### Порт занят

**Проблема:** `Error: listen EADDRINUSE: address already in use :::3000`

**Решение 1:** Найти и убить процесс
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3000
kill -9 <PID>
```

**Решение 2:** Использовать другой порт
```bash
npm run dev:3001
```

**Решение 3:** Vite автоматически выберет свободный порт
```
Vite автоматически попробует порты: 3000, 3001, 3002...
```

---

### localhost не работает

**Проблема:** `ERR_CONNECTION_REFUSED` при открытии localhost

**Решение 1:** Использовать 127.0.0.1
```
http://127.0.0.1:3000
```

**Решение 2:** Использовать 0.0.0.0
```
http://0.0.0.0:3000
```

**Решение 3:** Проверить hosts файл
```bash
# Windows: C:\Windows\System32\drivers\etc\hosts
# Linux/Mac: /etc/hosts

# Должна быть строка:
127.0.0.1 localhost
```

---

### Не могу подключиться с другого устройства

**Проблема:** Хочу открыть на телефоне/планшете

**Решение:** Используйте сетевой IP

1. Запустите сервер и клиент
2. Посмотрите сетевой IP в консоли
3. Откройте на другом устройстве: `http://192.168.1.100:3000`

⚠️ **Важно:** Устройства должны быть в одной сети!

---

## 📱 Все доступные URL

После запуска проект доступен по адресам:

### Клиент (Frontend)
```
http://localhost:3000
http://127.0.0.1:3000
http://0.0.0.0:3000
http://[ваш-IP]:3000
```

### Сервер (Backend API)
```
http://localhost:5000/api
http://127.0.0.1:5000/api
http://0.0.0.0:5000/api
http://[ваш-IP]:5000/api
```

### Примеры API запросов
```
http://localhost:5000/api/menu
http://127.0.0.1:5000/api/auth/check
http://192.168.1.100:5000/api/orders
```

---

## 🎯 Рекомендуемые комбинации

### Для разработки
```bash
Клиент: 3000 (или автоматический)
Сервер: 5000
```

### Если 3000 занят
```bash
Клиент: 3001, 8080, или 4000
Сервер: 5000
```

### Если 5000 занят
```bash
Клиент: 3000
Сервер: 5001 или 8000
```

### Для продакшена
```bash
Клиент: 80 (HTTP) или 443 (HTTPS)
Сервер: 5000 (за Nginx)
```

---

## 🔍 Проверка доступности портов

### Windows
```bash
# Проверить порт 3000
netstat -ano | findstr :3000

# Проверить порт 5000
netstat -ano | findstr :5000
```

### Linux/Mac
```bash
# Проверить порт 3000
lsof -i :3000

# Проверить порт 5000
lsof -i :5000

# Или
netstat -tuln | grep 3000
netstat -tuln | grep 5000
```

---

## 💡 Советы

1. **Vite автоматически выберет порт** если 3000 занят
2. **Используйте 127.0.0.1** если localhost не работает
3. **Проверьте firewall** если не можете подключиться
4. **Используйте сетевой IP** для доступа с других устройств
5. **Закройте другие приложения** использующие порты 3000/5000

---

## 🚀 Быстрый тест

Проверьте доступность после запуска:

```bash
# Тест клиента
curl http://localhost:3000
curl http://127.0.0.1:3000

# Тест сервера
curl http://localhost:5000/api/menu
curl http://127.0.0.1:5000/api/menu
```

Если получили ответ - всё работает! ✅

---

## 📞 Всё ещё не работает?

1. Проверьте, что оба терминала запущены
2. Проверьте консоль на ошибки
3. Попробуйте перезапустить
4. Попробуйте другой браузер
5. Проверьте антивирус/firewall
6. Попробуйте режим инкогнито

---

**Рекомендуемый способ запуска:**

```bash
# Терминал 1
npm run server

# Терминал 2  
npm run dev

# Откройте любой из адресов:
# - http://localhost:3000
# - http://127.0.0.1:3000
# - http://0.0.0.0:3000
```

**Готово!** 🎉
