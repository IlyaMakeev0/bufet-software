# 🔧 ИСПРАВЛЕНИЕ ОШИБКИ ПОДКЛЮЧЕНИЯ К БД

## Ошибка: `ECONNREFUSED ::1:5432`

### Причина:
Приложение запущено **локально** (npm run server), а база данных в **Docker контейнере**.

---

## ✅ РЕШЕНИЕ 1: Запустить все в Docker (рекомендуется)

### Шаг 1: Остановить локальный сервер
```bash
# Нажмите Ctrl+C в терминале где запущен npm run server
```

### Шаг 2: Перезапустить Docker
```bash
docker-compose down
docker-compose up -d
```

### Шаг 3: Проверить логи
```bash
docker logs school-canteen-app
```

### Шаг 4: Открыть приложение
```
http://localhost:5000
```

**Готово!** Все работает в Docker.

---

## ✅ РЕШЕНИЕ 2: Подключить локальное приложение к Docker БД

Если хотите запускать приложение локально (npm run server), но использовать Docker БД:

### Шаг 1: Создать .env файл
```bash
# Создайте файл .env в корне проекта
```

### Шаг 2: Добавить настройки
```env
NODE_ENV=development
PORT=3001

# Подключение к Docker БД
DB_HOST=localhost
DB_PORT=5432
DB_NAME=school_canteen
DB_USER=canteen_user
DB_PASSWORD=canteen_password_2024

SESSION_SECRET=школа-столовая-2024-секрет-ключ
```

### Шаг 3: Остановить Docker приложение (оставить только БД)
```bash
docker stop school-canteen-app
```

### Шаг 4: Запустить локально
```bash
npm run server
```

### Шаг 5: Открыть приложение
```
http://localhost:3001
```

---

## 🔍 ПРОВЕРКА ПОДКЛЮЧЕНИЯ

### Проверить, что БД работает:
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "SELECT 1;"
```

Должно вывести:
```
 ?column?
----------
        1
(1 row)
```

### Проверить логи БД:
```bash
docker logs school-canteen-db
```

### Проверить логи приложения:
```bash
docker logs school-canteen-app
```

---

## 🎯 РЕКОМЕНДАЦИЯ

**Используйте Решение 1** - запускайте все в Docker:
- ✅ Проще
- ✅ Надежнее
- ✅ Ближе к продакшену
- ✅ Нет проблем с подключением

**Используйте Решение 2** только если:
- Нужна горячая перезагрузка кода
- Активная разработка
- Отладка

---

## 📝 БЫСТРЫЕ КОМАНДЫ

### Перезапустить все в Docker:
```bash
docker-compose restart
```

### Пересобрать и запустить:
```bash
docker-compose down
docker-compose up -d --build
```

### Посмотреть статус:
```bash
docker ps
```

### Посмотреть логи:
```bash
docker logs -f school-canteen-app
```

---

## ✅ ПОСЛЕ ИСПРАВЛЕНИЯ

Тестовые аккаунты уже применены, можете сразу войти:
- student@test.com / test123
- chef@test.com / test123
- admin@test.com / test123

---

**Выберите решение и следуйте инструкциям!**
