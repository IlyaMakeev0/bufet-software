═══════════════════════════════════════════════════════════
  🔄 ПОЛНАЯ ОСТАНОВКА И ЗАПУСК
═══════════════════════════════════════════════════════════

Скопируйте и выполните на сервере:

# 1. Остановите всё
docker-compose -f docker-compose.production.yml down --remove-orphans

# 2. Удалите контейнеры вручную (если остались)
docker rm -f school-canteen-backend school-canteen-db

# 3. Пересоберите backend
docker-compose -f docker-compose.production.yml build --no-cache backend

# 4. Запустите всё
docker-compose -f docker-compose.production.yml up -d

# 5. Следите за логами
docker logs school-canteen-backend -f

═══════════════════════════════════════════════════════════

Нажмите Ctrl+C чтобы выйти из логов.

═══════════════════════════════════════════════════════════

Проверка:

docker ps

curl -k https://localhost:8443/health

═══════════════════════════════════════════════════════════
