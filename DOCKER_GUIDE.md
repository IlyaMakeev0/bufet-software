# 🐳 Руководство по Docker для School Canteen

## 📋 Содержание
1. [Архитектура](#архитектура)
2. [Предварительные требования](#предварительные-требования)
3. [Быстрый старт](#быстрый-старт)
4. [Подробная инструкция](#подробная-инструкция)
5. [Управление контейнерами](#управление-контейнерами)
6. [Troubleshooting](#troubleshooting)

---

## 🏗️ Архитектура

Проект разделен на два Docker-контейнера:

### 1. **База данных (PostgreSQL)**
- Контейнер: `school-canteen-db`
- Образ: `postgres:15-alpine`
- Порт: `5432`
- Volume: `postgres_data` (для сохранения данных)

### 2. **Приложение (Frontend + Backend)**
- Контейнер: `school-canteen-app`
- Образ: собирается из `Dockerfile`
- Порт: `5000`
- Включает:
  - React frontend (собранный Vite)
  - Express backend
  - API endpoints

### Сеть
Контейнеры связаны через Docker network `canteen-network`

---

## 📦 Предварительные требования

### Установка Docker

#### Windows
1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Запустите установщик
3. Перезагрузите компьютер
4. Запустите Docker Desktop

#### Linux
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
```

#### macOS
1. Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop/)
2. Установите приложение
3. Запустите Docker Desktop

### Проверка установки
```bash
docker --version
docker-compose --version
```

---

## 🚀 Быстрый старт

### 1. Клонирование и подготовка
```bash
# Перейдите в директорию проекта
cd school-canteen

# Установите зависимости (для локальной разработки)
npm install
```

### 2. Сборка и запуск
```bash
# Сборка образов и запуск контейнеров
docker-compose up --build

# Или в фоновом режиме
docker-compose up --build -d
```

### 3. Доступ к приложению
Откройте браузер и перейдите по адресу:
- **Приложение**: http://localhost:5000
- **API**: http://localhost:5000/api
- **Health check**: http://localhost:5000/health

### 4. Остановка
```bash
# Остановить контейнеры
docker-compose down

# Остановить и удалить volumes (БД будет очищена!)
docker-compose down -v
```

---

## 📖 Подробная инструкция

### Шаг 1: Подготовка проекта

#### 1.1 Проверка файлов
Убедитесь, что в проекте есть следующие файлы:
- `Dockerfile` - конфигурация образа приложения
- `docker-compose.yml` - оркестрация контейнеров
- `.dockerignore` - исключения при сборке
- `.env.docker` - переменные окружения
- `server/database-postgres.js` - адаптер для PostgreSQL
- `server/index-docker.js` - точка входа для Docker

#### 1.2 Настройка переменных окружения (опционально)
Отредактируйте `.env.docker` если нужно изменить настройки:
```env
DB_PASSWORD=ваш_пароль
PORT=5000
```

### Шаг 2: Сборка образов

#### 2.1 Сборка образа приложения
```bash
# Сборка только образа приложения
docker build -t school-canteen-app .

# Проверка созданного образа
docker images | grep school-canteen
```

#### 2.2 Сборка через docker-compose
```bash
# Сборка всех сервисов
docker-compose build

# Сборка без кэша (если были проблемы)
docker-compose build --no-cache
```

### Шаг 3: Запуск контейнеров

#### 3.1 Первый запуск
```bash
# Запуск с выводом логов в консоль
docker-compose up

# Вы увидите:
# ✅ PostgreSQL запустился
# ✅ База данных инициализирована
# ✅ Приложение запустилось на порту 5000
```

#### 3.2 Запуск в фоновом режиме
```bash
# Запуск в detached mode
docker-compose up -d

# Проверка статуса
docker-compose ps
```

#### 3.3 Просмотр логов
```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs app
docker-compose logs db

# Следить за логами в реальном времени
docker-compose logs -f app
```

### Шаг 4: Проверка работы

#### 4.1 Проверка контейнеров
```bash
# Список запущенных контейнеров
docker ps

# Должны быть запущены:
# - school-canteen-app
# - school-canteen-db
```

#### 4.2 Проверка здоровья
```bash
# Health check приложения
curl http://localhost:5000/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2024-01-24T..."}
```

#### 4.3 Проверка базы данных
```bash
# Подключение к PostgreSQL
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen

# Внутри psql:
\dt                    # Список таблиц
SELECT COUNT(*) FROM menu;  # Проверка данных
\q                     # Выход
```

#### 4.4 Проверка в браузере
1. Откройте http://localhost:5000
2. Должна загрузиться главная страница
3. Попробуйте зарегистрироваться/войти

---

## 🎮 Управление контейнерами

### Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Остановка и удаление контейнеров
docker-compose down

# Остановка и удаление контейнеров + volumes
docker-compose down -v

# Пересборка и запуск
docker-compose up --build -d
```

### Работа с отдельными сервисами

```bash
# Запуск только БД
docker-compose up -d db

# Перезапуск только приложения
docker-compose restart app

# Логи только БД
docker-compose logs -f db
```

### Выполнение команд внутри контейнеров

```bash
# Bash в контейнере приложения
docker exec -it school-canteen-app sh

# Bash в контейнере БД
docker exec -it school-canteen-db sh

# Выполнение команды без входа
docker exec school-canteen-app node --version
```

### Очистка

```bash
# Удалить остановленные контейнеры
docker container prune

# Удалить неиспользуемые образы
docker image prune

# Удалить неиспользуемые volumes
docker volume prune

# Полная очистка (осторожно!)
docker system prune -a --volumes
```

---

## 🔧 Troubleshooting

### Проблема: Порт уже занят

**Ошибка:**
```
Error: bind: address already in use
```

**Решение:**
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :5000
kill -9 <PID>

# Или измените порт в docker-compose.yml:
ports:
  - "5001:5000"  # Внешний порт 5001
```

### Проблема: База данных не инициализируется

**Решение:**
```bash
# Остановить и удалить volumes
docker-compose down -v

# Пересоздать и запустить
docker-compose up --build
```

### Проблема: Контейнер приложения падает

**Диагностика:**
```bash
# Проверить логи
docker-compose logs app

# Проверить статус
docker-compose ps

# Проверить здоровье БД
docker-compose exec db pg_isready -U canteen_user
```

**Решение:**
```bash
# Пересобрать без кэша
docker-compose build --no-cache app
docker-compose up -d
```

### Проблема: Не могу подключиться к БД

**Проверка:**
```bash
# Проверить сеть
docker network ls
docker network inspect school-canteen_canteen-network

# Проверить переменные окружения
docker exec school-canteen-app env | grep DB_
```

**Решение:**
Убедитесь, что в `.env.docker` правильные настройки:
```env
DB_HOST=db  # Имя сервиса из docker-compose.yml
DB_PORT=5432
```

### Проблема: Frontend не загружается

**Проверка:**
```bash
# Проверить наличие собранных файлов
docker exec school-canteen-app ls -la /app/dist

# Проверить логи сборки
docker-compose logs app | grep "vite build"
```

**Решение:**
```bash
# Пересобрать с нуля
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Медленная работа на Windows

**Решение:**
1. Убедитесь, что WSL 2 включен
2. В Docker Desktop: Settings → General → Use WSL 2
3. Переместите проект в WSL файловую систему:
```bash
# В WSL терминале
cd ~
git clone <your-repo>
cd school-canteen
docker-compose up --build
```

---

## 📊 Мониторинг

### Использование ресурсов
```bash
# Статистика контейнеров
docker stats

# Использование дискового пространства
docker system df
```

### Логи
```bash
# Последние 100 строк
docker-compose logs --tail=100

# Логи за последний час
docker-compose logs --since 1h

# Экспорт логов в файл
docker-compose logs > logs.txt
```

---

## 🔄 Обновление приложения

### После изменения кода

```bash
# 1. Остановить контейнеры
docker-compose down

# 2. Пересобрать образ
docker-compose build app

# 3. Запустить
docker-compose up -d

# Или одной командой:
docker-compose up --build -d
```

### После изменения зависимостей (package.json)

```bash
# Пересобрать без кэша
docker-compose build --no-cache app
docker-compose up -d
```

---

## 🌐 Production Deployment

### Рекомендации для production

1. **Используйте .env файл для секретов**
```bash
# Создайте .env файл (не коммитьте в git!)
cp .env.docker .env
# Измените пароли и секреты
```

2. **Настройте reverse proxy (nginx)**
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

3. **Настройте автоматический перезапуск**
```yaml
# В docker-compose.yml уже настроено:
restart: unless-stopped
```

4. **Настройте backup базы данных**
```bash
# Создание backup
docker exec school-canteen-db pg_dump -U canteen_user school_canteen > backup.sql

# Восстановление
docker exec -i school-canteen-db psql -U canteen_user school_canteen < backup.sql
```

---

## 📝 Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [PostgreSQL Docker Image](https://hub.docker.com/_/postgres)
- [Node.js Docker Best Practices](https://github.com/nodejs/docker-node/blob/main/docs/BestPractices.md)

---

## ✅ Чеклист проверки

После запуска убедитесь:

- [ ] `docker ps` показывает 2 запущенных контейнера
- [ ] http://localhost:5000 открывается
- [ ] http://localhost:5000/health возвращает `{"status":"ok"}`
- [ ] Можно зарегистрировать нового пользователя
- [ ] Можно войти в систему
- [ ] База данных содержит данные меню
- [ ] Логи не содержат ошибок

---

**Готово! 🎉** Ваше приложение работает в Docker-контейнерах.
