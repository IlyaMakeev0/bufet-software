# 🚀 ДЕПЛОЙ НА RENDER.COM

## Пошаговая инструкция

---

## 📋 ЧТО НУЖНО ПЕРЕД НАЧАЛОМ

1. ✅ Аккаунт на [Render.com](https://render.com)
2. ✅ Репозиторий на GitHub с вашим кодом
3. ✅ Файл `render.yaml` (уже создан)

---

## 🎯 ВАРИАНТ 1: Автоматический деплой (рекомендуется)

### Шаг 1: Загрузите код на GitHub

```bash
git add .
git commit -m "Add Render configuration"
git push origin main
```

### Шаг 2: Создайте Blueprint на Render

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически найдет `render.yaml`
5. Нажмите **"Apply"**

### Шаг 3: Дождитесь деплоя

Render автоматически:
- ✅ Создаст PostgreSQL базу данных
- ✅ Создаст веб-сервис
- ✅ Настроит переменные окружения
- ✅ Запустит приложение

### Шаг 4: Примените тестовые аккаунты

После успешного деплоя:

```bash
# Получите DATABASE_URL из Render Dashboard
# Затем выполните:
psql "ваш_DATABASE_URL" < create-test-accounts.sql
```

---

## 🎯 ВАРИАНТ 2: Ручной деплой

### Шаг 1: Создайте PostgreSQL базу

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   - **Name:** school-canteen-db
   - **Database:** school_canteen
   - **User:** canteen_user
   - **Region:** Frankfurt (или ближайший)
   - **Plan:** Free
4. Нажмите **"Create Database"**
5. **Сохраните** Internal Database URL

### Шаг 2: Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий
3. Заполните:
   - **Name:** school-canteen-app
   - **Region:** Frankfurt
   - **Branch:** main
   - **Root Directory:** (оставьте пустым)
   - **Runtime:** Node
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Plan:** Free

### Шаг 3: Настройте переменные окружения

В разделе **Environment Variables** добавьте:

```env
NODE_ENV=production
PORT=10000

# Скопируйте из PostgreSQL базы:
DB_HOST=<из Internal Database URL>
DB_PORT=5432
DB_NAME=school_canteen
DB_USER=canteen_user
DB_PASSWORD=<из Internal Database URL>
DATABASE_URL=<Internal Database URL>

# Сгенерируйте случайную строку:
SESSION_SECRET=<случайная_строка_минимум_32_символа>
```

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения сборки
3. Откройте URL вашего приложения

---

## 🔧 НАСТРОЙКА БАЗЫ ДАННЫХ

### После первого деплоя:

1. Откройте **PostgreSQL** в Render Dashboard
2. Нажмите **"Connect"** → **"External Connection"**
3. Скопируйте команду подключения
4. Выполните в терминале:

```bash
# Подключитесь к БД
psql "ваш_EXTERNAL_DATABASE_URL"

# Проверьте таблицы
\dt

# Если таблиц нет - они создадутся автоматически при первом запуске
```

### Применить тестовые аккаунты:

```bash
# Через psql
psql "ваш_EXTERNAL_DATABASE_URL" < create-test-accounts.sql

# Или через Render Shell
# В Dashboard → Web Service → Shell
cat create-test-accounts.sql | psql $DATABASE_URL
```

---

## 📊 ПРОВЕРКА ДЕПЛОЯ

### 1. Проверьте логи приложения:

В Render Dashboard → Web Service → Logs

Должны увидеть:
```
✅ PostgreSQL is ready!
✅ Database initialized
🚀 Server running on port 10000
```

### 2. Откройте приложение:

```
https://school-canteen-app.onrender.com
```

### 3. Войдите с тестовым аккаунтом:

```
Email: student@test.com
Пароль: test123
```

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### Free Plan ограничения:

1. **База данных:**
   - ✅ 1 GB хранилища
   - ✅ Автоматические бэкапы (90 дней)
   - ⚠️ Удаляется через 90 дней неактивности

2. **Web Service:**
   - ✅ 512 MB RAM
   - ✅ Автоматический SSL
   - ⚠️ Засыпает после 15 минут неактивности
   - ⚠️ Первый запрос после сна может занять 30-60 секунд

### Решение проблемы "засыпания":

Используйте сервис мониторинга (например, UptimeRobot):
- Пингует ваш сайт каждые 5 минут
- Держит приложение активным
- Бесплатно

---

## 🔐 БЕЗОПАСНОСТЬ

### Обязательно:

1. **Смените SESSION_SECRET:**
   ```bash
   # Сгенерируйте случайную строку
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

2. **Используйте переменные окружения:**
   - Не храните пароли в коде
   - Используйте Render Environment Variables

3. **Настройте CORS:**
   - Укажите правильный FRONTEND_URL
   - Ограничьте доступ к API

---

## 🐛 TROUBLESHOOTING

### Ошибка: "Cannot connect to database"

**Решение:**
1. Проверьте переменные окружения (DB_HOST, DB_PORT, etc.)
2. Убедитесь, что база данных запущена
3. Проверьте Internal Database URL

### Ошибка: "Build failed"

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что `package.json` корректен
3. Проверьте версию Node.js (должна быть 18+)

### Приложение не отвечает

**Решение:**
1. Проверьте логи: Dashboard → Logs
2. Перезапустите сервис: Dashboard → Manual Deploy → Deploy latest commit
3. Проверьте статус базы данных

### База данных пустая

**Решение:**
1. Приложение создаст таблицы автоматически при первом запуске
2. Если нет - проверьте логи инициализации
3. Примените тестовые аккаунты вручную

---

## 📝 ПОЛЕЗНЫЕ КОМАНДЫ

### Подключение к БД:
```bash
psql "ваш_EXTERNAL_DATABASE_URL"
```

### Проверка таблиц:
```sql
\dt
```

### Проверка пользователей:
```sql
SELECT email, role, email_verified FROM users;
```

### Применить тестовые аккаунты:
```bash
psql "ваш_EXTERNAL_DATABASE_URL" < create-test-accounts.sql
```

### Очистить БД:
```sql
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
```

---

## 🎯 ПОСЛЕ ДЕПЛОЯ

### 1. Проверьте функциональность:
- [ ] Главная страница открывается
- [ ] Вход с тестовым аккаунтом работает
- [ ] Меню отображается
- [ ] Заказы создаются
- [ ] Подписки оформляются

### 2. Настройте домен (опционально):
- Render предоставляет бесплатный поддомен
- Можно подключить свой домен

### 3. Настройте мониторинг:
- UptimeRobot для пингов
- Render Metrics для статистики

---

## 🌐 ПОЛЕЗНЫЕ ССЫЛКИ

- [Render Documentation](https://render.com/docs)
- [Render PostgreSQL Guide](https://render.com/docs/databases)
- [Render Environment Variables](https://render.com/docs/environment-variables)
- [Render Free Plan](https://render.com/docs/free)

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ

- [ ] Код загружен на GitHub
- [ ] Создан `render.yaml`
- [ ] Создана PostgreSQL база на Render
- [ ] Создан Web Service на Render
- [ ] Настроены переменные окружения
- [ ] Приложение успешно задеплоено
- [ ] База данных инициализирована
- [ ] Тестовые аккаунты применены
- [ ] Вход работает
- [ ] Все функции проверены

---

**Готово! Ваше приложение на Render!** 🎉
