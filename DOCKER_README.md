# 🐳 Docker Architecture Overview

## Архитектура проекта

```
┌─────────────────────────────────────────────────────────────┐
│                     Docker Compose                          │
│                                                             │
│  ┌──────────────────────┐      ┌──────────────────────┐   │
│  │   Container: app     │      │   Container: db      │   │
│  │                      │      │                      │   │
│  │  ┌────────────────┐  │      │  ┌────────────────┐  │   │
│  │  │  React (Vite)  │  │      │  │  PostgreSQL 15 │  │   │
│  │  │   Frontend     │  │      │  │                │  │   │
│  │  └────────────────┘  │      │  │  Port: 5432    │  │   │
│  │                      │      │  │                │  │   │
│  │  ┌────────────────┐  │      │  │  Volume:       │  │   │
│  │  │  Express.js    │◄─┼──────┼─►│  postgres_data │  │   │
│  │  │   Backend      │  │      │  │                │  │   │
│  │  │                │  │      │  └────────────────┘  │   │
│  │  │  Port: 5000    │  │      │                      │   │
│  │  └────────────────┘  │      └──────────────────────┘   │
│  │                      │                                  │
│  └──────────────────────┘                                  │
│           │                                                 │
│           │ Port Mapping                                    │
│           ▼                                                 │
│    localhost:5000                                           │
└─────────────────────────────────────────────────────────────┘
```

## Компоненты

### 1. База данных (PostgreSQL)
- **Образ**: `postgres:15-alpine`
- **Контейнер**: `school-canteen-db`
- **Порт**: 5432
- **Данные**: Сохраняются в Docker volume `postgres_data`
- **Конфигурация**:
  - Database: `school_canteen`
  - User: `canteen_user`
  - Password: `canteen_password_2024`

### 2. Приложение
- **Образ**: Собирается из `Dockerfile`
- **Контейнер**: `school-canteen-app`
- **Порт**: 5000
- **Компоненты**:
  - React frontend (собранный)
  - Express backend
  - API endpoints

## Файловая структура Docker

```
school-canteen/
├── Dockerfile                    # Конфигурация образа приложения
├── docker-compose.yml            # Оркестрация контейнеров
├── .dockerignore                 # Исключения при сборке
├── .env.docker                   # Переменные окружения
├── init-db.sql                   # Инициализация БД
│
├── server/
│   ├── index-docker.js           # Точка входа для Docker
│   ├── database-postgres.js      # Адаптер PostgreSQL
│   └── database.js               # Адаптер SQLite (для локальной разработки)
│
├── DOCKER_GUIDE.md               # Подробная документация
├── DOCKER_QUICKSTART.md          # Быстрый старт
│
└── Windows Scripts:
    ├── docker-start.bat          # Запуск
    ├── docker-stop.bat           # Остановка
    ├── docker-rebuild.bat        # Пересборка
    └── docker-logs.bat           # Просмотр логов
```

## Процесс сборки (Multi-stage build)

### Stage 1: Frontend Builder
```dockerfile
FROM node:18-alpine AS frontend-builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
```
- Устанавливает зависимости
- Собирает React приложение с Vite
- Создает оптимизированные статические файлы в `/app/dist`

### Stage 2: Production Image
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY server ./server
COPY --from=frontend-builder /app/dist ./dist
```
- Использует только production зависимости
- Копирует backend код
- Копирует собранный frontend из Stage 1
- Запускает `server/index-docker.js`

## Сеть и коммуникация

### Docker Network: `canteen-network`
- Тип: bridge
- Контейнеры могут обращаться друг к другу по имени сервиса
- Приложение подключается к БД через `db:5432`

### Порты
- **5000** - Приложение (HTTP)
- **5432** - PostgreSQL (только для отладки)

## Volumes (Постоянное хранилище)

### postgres_data
- Хранит данные PostgreSQL
- Сохраняется между перезапусками
- Удаляется только при `docker-compose down -v`

### app_data
- Хранит данные приложения (логи, кэш)
- Опционально

## Переменные окружения

### Приложение (app)
```env
NODE_ENV=production
PORT=5000
DB_HOST=db
DB_PORT=5432
DB_NAME=school_canteen
DB_USER=canteen_user
DB_PASSWORD=canteen_password_2024
FRONTEND_URL=http://localhost:5000
SESSION_SECRET=...
```

### База данных (db)
```env
POSTGRES_DB=school_canteen
POSTGRES_USER=canteen_user
POSTGRES_PASSWORD=canteen_password_2024
```

## Health Checks

### База данных
```yaml
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U canteen_user -d school_canteen"]
  interval: 10s
  timeout: 5s
  retries: 5
```

### Приложение
- Endpoint: `GET /health`
- Ответ: `{"status":"ok","timestamp":"..."}`

## Зависимости запуска

```yaml
app:
  depends_on:
    db:
      condition: service_healthy
```
- Приложение запускается только после того, как БД готова
- Проверяется через health check

## Оптимизации

### Размер образа
- Использование `alpine` образов (меньше размер)
- Multi-stage build (только необходимые файлы)
- `.dockerignore` (исключение ненужных файлов)

### Производительность
- Кэширование слоев Docker
- Установка зависимостей перед копированием кода
- Production-only зависимости в финальном образе

### Безопасность
- Не хранить секреты в образе
- Использовать переменные окружения
- Минимальные привилегии
- Регулярные обновления базовых образов

## Мониторинг

### Логи
```bash
# Все логи
docker-compose logs

# Конкретный сервис
docker-compose logs app
docker-compose logs db

# В реальном времени
docker-compose logs -f
```

### Статистика
```bash
# Использование ресурсов
docker stats

# Дисковое пространство
docker system df
```

## Backup и восстановление

### Backup базы данных
```bash
docker exec school-canteen-db pg_dump -U canteen_user school_canteen > backup.sql
```

### Восстановление
```bash
docker exec -i school-canteen-db psql -U canteen_user school_canteen < backup.sql
```

## Production Considerations

### Рекомендации для production:
1. Использовать secrets management (Docker Secrets, Vault)
2. Настроить reverse proxy (nginx, Traefik)
3. Включить HTTPS
4. Настроить автоматические backup
5. Мониторинг и алертинг (Prometheus, Grafana)
6. Логирование (ELK Stack, Loki)
7. Ограничение ресурсов контейнеров
8. Регулярные обновления образов

### Пример ограничения ресурсов:
```yaml
app:
  deploy:
    resources:
      limits:
        cpus: '1'
        memory: 512M
      reservations:
        cpus: '0.5'
        memory: 256M
```

## Troubleshooting

См. раздел Troubleshooting в [DOCKER_GUIDE.md](./DOCKER_GUIDE.md)

---

**Документация**: [DOCKER_GUIDE.md](./DOCKER_GUIDE.md)  
**Быстрый старт**: [DOCKER_QUICKSTART.md](./DOCKER_QUICKSTART.md)
