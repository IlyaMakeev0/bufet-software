# 🤝 Руководство для разработчиков

Спасибо за интерес к проекту! Это руководство поможет вам начать разработку.

## 📋 Содержание

- [Начало работы](#начало-работы)
- [Структура проекта](#структура-проекта)
- [Стандарты кода](#стандарты-кода)
- [Git workflow](#git-workflow)
- [Тестирование](#тестирование)
- [Отправка изменений](#отправка-изменений)

## 🚀 Начало работы

### Требования

- Node.js 16+
- npm или yarn
- Git
- Редактор кода (VS Code рекомендуется)

### Установка

```bash
# Форкнуть репозиторий на GitHub

# Клонировать ваш форк
git clone https://github.com/YOUR_USERNAME/school-canteen.git
cd school-canteen

# Добавить upstream
git remote add upstream https://github.com/ORIGINAL_OWNER/school-canteen.git

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run server  # Терминал 1
npm run dev     # Терминал 2
```

## 📁 Структура проекта

```
school-canteen/
├── src/                    # Frontend
│   ├── components/         # React компоненты
│   ├── pages/             # Страницы
│   ├── App.jsx            # Главный компонент
│   └── main.jsx           # Точка входа
│
├── server/                # Backend
│   ├── routes/            # API роуты
│   ├── utils/             # Утилиты
│   ├── database.js        # БД
│   └── index.js           # Сервер
│
└── docs/                  # Документация
```

## 📝 Стандарты кода

### JavaScript/JSX

```javascript
// ✅ Хорошо
function StudentDashboard({ user }) {
  const [orders, setOrders] = useState([])
  
  useEffect(() => {
    loadOrders()
  }, [])
  
  const loadOrders = async () => {
    try {
      const res = await fetch('/api/orders')
      if (res.ok) {
        setOrders(await res.json())
      }
    } catch (error) {
      console.error('Failed to load orders:', error)
    }
  }
  
  return (
    <div className="dashboard">
      {/* Контент */}
    </div>
  )
}

// ❌ Плохо
function studentDashboard(props) {
  var orders = []
  
  fetch('/api/orders').then(res => {
    orders = res.json()
  })
  
  return <div>{/* Контент */}</div>
}
```

### Именование

```javascript
// Компоненты - PascalCase
StudentDashboard
ChefAuth
AdminPanel

// Функции - camelCase
loadOrders()
createOrder()
validatePhone()

// Константы - UPPER_SNAKE_CASE
const API_URL = 'http://localhost:5000'
const MAX_ORDERS = 100

// Файлы компонентов - PascalCase
StudentDashboard.jsx
ChefAuth.jsx

// Файлы утилит - camelCase
phone.js
validation.js
```

### CSS

```css
/* ✅ Хорошо - BEM методология */
.dashboard {}
.dashboard__header {}
.dashboard__content {}
.dashboard--loading {}

/* Или модульные классы */
.card {}
.card-header {}
.card-body {}

/* ❌ Плохо */
.div1 {}
.myDiv {}
.DashboardHeader {}
```

### Комментарии

```javascript
// ✅ Хорошо - объясняют "почему"
// Используем setTimeout для debounce поиска
setTimeout(() => search(query), 300)

// Проверяем баланс перед созданием заказа
if (balance < price) {
  return res.status(400).json({ error: 'Недостаточно средств' })
}

// ❌ Плохо - объясняют "что" (очевидно из кода)
// Создаем переменную orders
const orders = []

// Вызываем функцию loadOrders
loadOrders()
```

## 🔀 Git Workflow

### Ветки

```bash
# Основные ветки
main          # Продакшен
develop       # Разработка

# Feature ветки
feature/add-reviews
feature/improve-ui
feature/add-notifications

# Bugfix ветки
bugfix/fix-login
bugfix/fix-balance

# Hotfix ветки
hotfix/security-patch
```

### Коммиты

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Формат
<type>(<scope>): <subject>

# Типы
feat:     новая функция
fix:      исправление бага
docs:     документация
style:    форматирование
refactor: рефакторинг
test:     тесты
chore:    обслуживание

# Примеры
feat(auth): add phone validation
fix(orders): correct balance calculation
docs(readme): update installation steps
style(dashboard): improve card spacing
refactor(api): simplify error handling
test(auth): add login tests
chore(deps): update dependencies
```

### Workflow

```bash
# 1. Обновить main
git checkout main
git pull upstream main

# 2. Создать feature ветку
git checkout -b feature/add-reviews

# 3. Внести изменения
# ... код ...

# 4. Коммит
git add .
git commit -m "feat(reviews): add review system"

# 5. Push
git push origin feature/add-reviews

# 6. Создать Pull Request на GitHub
```

## 🧪 Тестирование

### Ручное тестирование

Перед отправкой PR проверьте:

- [ ] Регистрация работает для всех ролей
- [ ] Вход работает для всех ролей
- [ ] Создание заказа работает
- [ ] Баланс обновляется корректно
- [ ] Выдача блюд работает
- [ ] Статистика отображается правильно
- [ ] Нет ошибок в консоли
- [ ] Адаптивность на мобильных

### Автоматическое тестирование

```bash
# Запустить тесты (когда будут добавлены)
npm test

# Проверить покрытие
npm run test:coverage

# Линтинг
npm run lint
```

## 📤 Отправка изменений

### Чеклист перед PR

- [ ] Код соответствует стандартам
- [ ] Нет console.log (кроме важных)
- [ ] Нет закомментированного кода
- [ ] Добавлены комментарии где нужно
- [ ] Обновлена документация
- [ ] Протестировано вручную
- [ ] Нет конфликтов с main
- [ ] Коммиты осмысленные

### Создание Pull Request

1. **Заголовок**: краткое описание
   ```
   feat: Add review system for meals
   ```

2. **Описание**: подробности
   ```markdown
   ## Что изменено
   - Добавлена таблица reviews в БД
   - Создан компонент ReviewForm
   - Добавлен API endpoint для отзывов
   
   ## Как тестировать
   1. Войти как ученик
   2. Заказать блюдо
   3. Оставить отзыв
   4. Проверить отображение
   
   ## Скриншоты
   ![Review Form](screenshot.png)
   ```

3. **Связанные issues**
   ```
   Closes #123
   Related to #456
   ```

### Code Review

Будьте готовы к:
- Вопросам о реализации
- Предложениям по улучшению
- Запросам на изменения

Отвечайте конструктивно и вежливо.

## 🎨 UI/UX Guidelines

### Цвета

```css
/* Основные */
--primary: #3498db;
--success: #2ecc71;
--danger: #e74c3c;
--warning: #f39c12;
--secondary: #95a5a6;

/* Текст */
--text-dark: #2c3e50;
--text-light: #7f8c8d;

/* Фон */
--bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### Отступы

```css
/* Используйте кратные 4px */
padding: 4px;   /* Маленький */
padding: 8px;   /* Средний */
padding: 12px;  /* Обычный */
padding: 16px;  /* Большой */
padding: 20px;  /* Очень большой */
```

### Шрифты

```css
/* Размеры */
font-size: 0.85em;  /* Маленький */
font-size: 1em;     /* Обычный */
font-size: 1.2em;   /* Средний */
font-size: 1.5em;   /* Большой */
font-size: 2em;     /* Заголовок */
```

## 🐛 Отчеты об ошибках

### Шаблон issue

```markdown
## Описание
Краткое описание проблемы

## Шаги воспроизведения
1. Перейти на страницу X
2. Нажать кнопку Y
3. Увидеть ошибку Z

## Ожидаемое поведение
Что должно было произойти

## Фактическое поведение
Что произошло на самом деле

## Скриншоты
Если применимо

## Окружение
- OS: Windows 10
- Browser: Chrome 120
- Node: 18.0.0
```

## 💡 Предложения функций

### Шаблон feature request

```markdown
## Проблема
Какую проблему решает эта функция?

## Решение
Как вы предлагаете решить проблему?

## Альтернативы
Какие альтернативные решения рассматривались?

## Дополнительно
Любая дополнительная информация
```

## 📚 Полезные ресурсы

### Документация
- [React](https://react.dev/)
- [Express](https://expressjs.com/)
- [SQLite](https://www.sqlite.org/)
- [Vite](https://vitejs.dev/)

### Инструменты
- [VS Code](https://code.visualstudio.com/)
- [React DevTools](https://react.dev/learn/react-developer-tools)
- [Postman](https://www.postman.com/)

### Обучение
- [React Tutorial](https://react.dev/learn)
- [Node.js Guide](https://nodejs.org/en/docs/guides/)
- [JavaScript.info](https://javascript.info/)

## 🤔 Вопросы?

- Создайте issue с меткой `question`
- Напишите в Discussions
- Свяжитесь с мейнтейнерами

## 📜 Код поведения

- Будьте уважительны
- Будьте конструктивны
- Помогайте другим
- Следуйте стандартам

## 🎉 Спасибо!

Ваш вклад делает проект лучше!

---

**Последнее обновление:** 23 января 2024
