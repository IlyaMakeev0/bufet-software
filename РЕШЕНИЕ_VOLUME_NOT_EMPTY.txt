╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ РЕШЕНИЕ: Volume не пустой                           ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ПРОБЛЕМА:                                               │
└───────────────────────────────────────────────────────────┘

directory "/var/lib/postgresql/data" exists but is not empty

Volume PostgreSQL не был полностью очищен.

┌───────────────────────────────────────────────────────────┐
│  🚀 РЕШЕНИЕ (выполните на сервере):                      │
└───────────────────────────────────────────────────────────┘

# 1. Остановите ВСЕ контейнеры
docker-compose -f docker-compose.production.yml down

# 2. Удалите контейнер PostgreSQL принудительно
docker rm -f school-canteen-db

# 3. Найдите volume
docker volume ls | grep postgres

# 4. Удалите volume принудительно
docker volume rm -f bufet-software_postgres_data

# 5. Если не удаляется, удалите ВСЕ неиспользуемые volumes
docker volume prune -f

# 6. Проверьте, что volume удален
docker volume ls

# 7. Запустите заново
docker-compose -f docker-compose.production.yml up -d

# 8. Следите за логами
docker logs school-canteen-backend -f

┌───────────────────────────────────────────────────────────┐
│  ⚡ АЛЬТЕРНАТИВА - ПОЛНАЯ ОЧИСТКА:                       │
└───────────────────────────────────────────────────────────┘

Если volume всё равно не удаляется:

# 1. Остановите Docker
sudo systemctl stop docker

# 2. Удалите volume вручную
sudo rm -rf /var/lib/docker/volumes/bufet-software_postgres_data

# 3. Запустите Docker
sudo systemctl start docker

# 4. Проверьте
docker volume ls

# 5. Запустите проект
docker-compose -f docker-compose.production.yml up -d

┌───────────────────────────────────────────────────────────┐
│  🔍 ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ ПОСЛЕ ЗАПУСКА:               │
└───────────────────────────────────────────────────────────┘

PostgreSQL:
   creating directory /var/lib/postgresql/data ... ok
   creating subdirectories ... ok
   selecting dynamic shared memory implementation ... posix
   ...
   database system is ready to accept connections

Backend:
   ⏳ Waiting for PostgreSQL...
   ✅ Database initialized
   🚀 HTTPS Server running on port 8443

┌───────────────────────────────────────────────────────────┐
│  📊 ПРОВЕРКА:                                            │
└───────────────────────────────────────────────────────────┘

# Статус
docker ps

Должно быть:
   school-canteen-backend   Up X seconds
   school-canteen-db        Up X seconds

# Проверка приложения
curl -k https://localhost:8443/health

Ответ:
   {"status":"ok","timestamp":"...","ssl":true}

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ ПОСЛЕ ЭТОГО ВСЁ ДОЛЖНО ЗАРАБОТАТЬ                   ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
