# 🔍 Диагностика ошибок отправки email

## 📋 Что было добавлено

Добавлено подробное логирование для диагностики проблем с отправкой email:

### В `server/routes/auth.js`:
- ✅ Логирование попытки отправки
- ✅ Показ сгенерированного кода в консоли
- ✅ Подробное описание ошибки
- ✅ Рекомендация использовать тестовые аккаунты

### В `server/utils/emailService.js`:
- ✅ Логирование подготовки письма
- ✅ Логирование процесса отправки
- ✅ Детальная информация об ошибках
- ✅ Показ SMTP ответов

---

## 🔍 Как диагностировать проблему

### Шаг 1: Запустите сервер и смотрите логи

```bash
npm run server
```

При запуске вы должны увидеть:

```
📧 Email сервис настроен:
   Host: smtp.gmail.com
   Port: 587
   User: ppredprof@gmail.com
   Password: ***
   Secure: false (используется STARTTLS)
```

### Шаг 2: Попробуйте отправить код

1. Откройте http://localhost/student/register
2. Введите email
3. Нажмите "Получить код"
4. **Смотрите консоль сервера**

### Шаг 3: Анализируйте логи

#### ✅ Успешная отправка:

```
📧 Попытка отправки кода на test@example.com
🔑 Код: 123456
📧 Подготовка письма для test@example.com
📤 Отправка письма через smtp.gmail.com:587
✅ Email sent successfully: <message-id>
📬 Response: 250 2.0.0 OK
```

#### ❌ Ошибка авторизации:

```
📧 Попытка отправки кода на test@example.com
🔑 Код: 123456
📧 Подготовка письма для test@example.com
📤 Отправка письма через smtp.gmail.com:587
❌ Error sending email:
   Message: Invalid login: 535-5.7.8 Username and Password not accepted
   Code: EAUTH
   Command: AUTH PLAIN
```

**Решение:** Неправильный пароль приложения

#### ❌ Ошибка подключения:

```
📧 Попытка отправки кода на test@example.com
🔑 Код: 123456
📧 Подготовка письма для test@example.com
📤 Отправка письма через smtp.gmail.com:587
❌ Error sending email:
   Message: connect ETIMEDOUT
   Code: ETIMEDOUT
   Command: CONN
```

**Решение:** Нет интернета или порт 587 заблокирован

#### ❌ Ошибка DNS:

```
❌ Error sending email:
   Message: getaddrinfo ENOTFOUND smtp.gmail.com
   Code: ENOTFOUND
```

**Решение:** Проблемы с DNS или интернетом

---

## 🛠️ Типичные ошибки и решения

### 1. Invalid login (EAUTH)

**Ошибка:**
```
535-5.7.8 Username and Password not accepted
```

**Причины:**
- Неправильный пароль приложения
- Пароль приложения не создан
- 2FA не включена в Gmail

**Решение:**
1. Включите 2FA: https://myaccount.google.com/security
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
3. Обновите в `server/utils/emailService.js`:
   ```javascript
   pass: 'xxxx xxxx xxxx xxxx' // Новый пароль
   ```
4. Перезапустите сервер

### 2. Connection timeout (ETIMEDOUT)

**Ошибка:**
```
connect ETIMEDOUT
```

**Причины:**
- Нет интернета
- Порт 587 заблокирован firewall
- Проблемы с сетью

**Решение:**
1. Проверьте интернет соединение
2. Проверьте firewall (разрешите порт 587)
3. Попробуйте другую сеть
4. Используйте тестовые аккаунты

### 3. DNS error (ENOTFOUND)

**Ошибка:**
```
getaddrinfo ENOTFOUND smtp.gmail.com
```

**Причины:**
- Нет интернета
- Проблемы с DNS

**Решение:**
1. Проверьте интернет
2. Попробуйте другую сеть
3. Проверьте DNS настройки

### 4. Too many connections

**Ошибка:**
```
421 4.7.0 Too many login attempts
```

**Причины:**
- Слишком много попыток отправки
- Gmail временно заблокировал доступ

**Решение:**
1. Подождите 15-30 минут
2. Используйте тестовые аккаунты
3. Проверьте безопасность аккаунта Gmail

### 5. Recipient rejected

**Ошибка:**
```
550 5.1.1 Recipient address rejected
```

**Причины:**
- Неправильный email адрес
- Email не существует

**Решение:**
1. Проверьте правильность email
2. Используйте существующий email
3. Попробуйте другой email

---

## 🔧 Быстрые решения

### Решение 1: Используйте тестовые аккаунты (рекомендуется)

Не нужно настраивать email! Просто войдите:

```
Email: student@test.com
Пароль: test123
```

**URL:** http://localhost/student/login

### Решение 2: Проверьте credentials

Откройте `server/utils/emailService.js` и проверьте:

```javascript
const SMTP_CONFIG = {
  host: 'smtp.gmail.com',
  port: 587,
  secure: false,
  auth: {
    user: 'ppredprof@gmail.com',      // ← Правильный email?
    pass: 'xvzr khqt hckc wabb'       // ← Правильный пароль?
  }
}
```

### Решение 3: Создайте новый пароль приложения

1. https://myaccount.google.com/apppasswords
2. Создайте новый пароль
3. Обновите в коде
4. Перезапустите сервер

### Решение 4: Проверьте интернет

```bash
# Windows
ping smtp.gmail.com

# Должно быть:
# Reply from 142.250.185.109: bytes=32 time=20ms TTL=117
```

### Решение 5: Проверьте порт 587

```bash
# Windows
telnet smtp.gmail.com 587

# Должно подключиться
```

---

## 📊 Таблица кодов ошибок

| Код | Описание | Решение |
|-----|----------|---------|
| EAUTH | Неправильный логин/пароль | Создайте новый пароль приложения |
| ETIMEDOUT | Таймаут подключения | Проверьте интернет и firewall |
| ENOTFOUND | DNS ошибка | Проверьте интернет |
| ECONNREFUSED | Соединение отклонено | Проверьте порт 587 |
| 535 | Неверные credentials | Обновите пароль приложения |
| 421 | Слишком много попыток | Подождите 30 минут |
| 550 | Неверный email | Проверьте адрес получателя |

---

## 🧪 Тестирование email

### Тест 1: Проверка подключения

Добавьте в `server/index.js`:

```javascript
import { verifyEmailConnection } from './utils/emailService.js'

// После запуска сервера
verifyEmailConnection().then(result => {
  if (result) {
    console.log('✅ Email сервис работает')
  } else {
    console.log('❌ Email сервис не работает')
  }
})
```

### Тест 2: Тестовая отправка

Создайте `test-email.js`:

```javascript
import { sendVerificationCode } from './server/utils/emailService.js'

const testEmail = 'ваш-email@gmail.com'
const testCode = '123456'

console.log('🧪 Тестовая отправка email...')

sendVerificationCode(testEmail, testCode)
  .then(() => {
    console.log('✅ Email отправлен успешно!')
    console.log('📧 Проверьте почту:', testEmail)
    process.exit(0)
  })
  .catch(err => {
    console.error('❌ Ошибка:', err.message)
    process.exit(1)
  })
```

Запустите:
```bash
node test-email.js
```

---

## 📝 Чеклист диагностики

Перед обращением за помощью, проверьте:

- [ ] Сервер запущен (`npm run server`)
- [ ] Есть интернет соединение
- [ ] Порт 587 не заблокирован
- [ ] Credentials правильные в `emailService.js`
- [ ] 2FA включена в Gmail
- [ ] Пароль приложения создан
- [ ] Email адрес правильный
- [ ] Логи сервера проверены
- [ ] Тестовые аккаунты работают

---

## 🎯 Рекомендации

### Для разработки:
✅ **Используйте тестовые аккаунты** - не нужно настраивать email
✅ `student@test.com` / `test123` - работает сразу
✅ Нет зависимости от интернета

### Для production:
⚠️ Настройте реальный SMTP
⚠️ Используйте переменные окружения
⚠️ Проверьте все credentials
⚠️ Тестируйте перед деплоем

---

## 📞 Получение помощи

### Если ничего не помогло:

1. **Скопируйте полный текст ошибки** из консоли сервера
2. **Сделайте скриншот** ошибки в браузере
3. **Опишите что делали** перед ошибкой
4. **Укажите версию** Node.js (`node --version`)

### Временное решение:

Используйте тестовые аккаунты:
```
student@test.com / test123
chef@test.com / test123
admin@test.com / test123
```

**URL:** http://localhost/student/login

---

**Обновлено:** 28 января 2026  
**Версия:** 1.0  
**Статус:** Добавлено подробное логирование
