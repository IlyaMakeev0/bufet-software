# 🔐 Система сброса пароля готова!

## ✅ Реализовано

Добавлена полноценная система восстановления пароля с двумя вариантами:
1. **Через Email** - получение кода на почту
2. **Через администрацию** - отправка запроса администратору

## 🎯 Функционал

### 1. Сброс пароля через Email

**Процесс:**
1. Пользователь нажимает "Забыли пароль?" на странице входа
2. Выбирает метод "Через Email"
3. Указывает роль (Ученик/Повар/Администратор) и email
4. Получает 6-значный код на почту
5. Вводит код и устанавливает новый пароль

**Особенности:**
- ✅ Код действителен 15 минут
- ✅ Красивое HTML-письмо с кодом
- ✅ Проверка существования пользователя
- ✅ Безопасное хеширование нового пароля
- ✅ Автоматическое удаление кода после использования

### 2. Сброс пароля через администрацию

**Процесс:**
1. Пользователь нажимает "Забыли пароль?" на странице входа
2. Выбирает метод "Через администрацию"
3. Указывает роль, email и причину запроса
4. Запрос сохраняется в базе данных
5. Администратор рассматривает запрос в своей панели
6. Пользователь получает ответ на email

**Особенности:**
- ✅ Запросы сохраняются в БД
- ✅ Статусы: pending, approved, rejected
- ✅ Администратор может добавить комментарий
- ✅ История всех запросов

## 📧 API Endpoints

### POST `/api/auth/request-password-reset`
Отправка кода сброса на email

**Request:**
```json
{
  "email": "user@example.com",
  "role": "student"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Код для сброса пароля отправлен на ваш email"
}
```

### POST `/api/auth/reset-password`
Сброс пароля с кодом

**Request:**
```json
{
  "email": "user@example.com",
  "code": "123456",
  "newPassword": "newpass123"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Пароль успешно изменён"
}
```

### POST `/api/auth/request-admin-password-reset`
Создание запроса администрации

**Request:**
```json
{
  "email": "user@example.com",
  "role": "student",
  "reason": "Забыл пароль, нет доступа к email"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Запрос на сброс пароля отправлен администрации",
  "requestId": "uuid"
}
```

## 🗄️ База данных

### Таблица `password_reset_requests`

```sql
CREATE TABLE password_reset_requests (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  email TEXT NOT NULL,
  role TEXT NOT NULL,
  reason TEXT NOT NULL,
  status TEXT DEFAULT 'pending',
  admin_comment TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

## 🎨 UI/UX

### Страница сброса пароля (`/forgot-password`)

**Шаг 1: Выбор метода**
- Две карточки с выбором метода
- Иконки и описания
- Красивая анимация при наведении

**Шаг 2: Ввод данных**
- Выбор роли (dropdown)
- Ввод email
- Для метода "Через администрацию" - поле причины

**Шаг 3: Ввод кода (только для email)**
- Поле для 6-значного кода
- Поля для нового пароля
- Подтверждение пароля

### Ссылки на страницах входа

На всех страницах входа добавлена ссылка:
- ✅ `/student/login` → "Забыли пароль?"
- ✅ `/chef/login` → "Забыли пароль?"
- ✅ `/admin/login` → "Забыли пароль?"

## 📧 Email шаблон

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔐 Школьная столовая
Сброс пароля
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Код для сброса пароля:

    1 2 3 4 5 6

⚠️ Важно:
• Код действителен 15 минут
• Никому не сообщайте этот код
• Если вы не запрашивали сброс,
  проигнорируйте это письмо

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
© 2026 Школьная столовая
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 🧪 Тестирование

### 1. Сброс через Email

```bash
# Откройте браузер
http://localhost:5000

# Шаги:
1. Нажмите "Вход для ученика"
2. Нажмите "Забыли пароль?"
3. Выберите "Через Email"
4. Укажите роль: Ученик
5. Введите email зарегистрированного пользователя
6. Нажмите "Отправить код"
7. Проверьте почту (включая "Спам")
8. Введите код из письма
9. Установите новый пароль
10. Войдите с новым паролем
```

### 2. Сброс через администрацию

```bash
# Шаги:
1. Нажмите "Вход для ученика"
2. Нажмите "Забыли пароль?"
3. Выберите "Через администрацию"
4. Укажите роль: Ученик
5. Введите email
6. Опишите причину запроса
7. Нажмите "Отправить запрос"
8. Ожидайте ответа администратора
```

## 🔐 Безопасность

### Реализовано:
- ✅ Код действует только 15 минут
- ✅ Код удаляется после использования
- ✅ Проверка существования пользователя
- ✅ Минимальная длина пароля: 6 символов
- ✅ Хеширование пароля с bcrypt
- ✅ Код не показывается в интерфейсе
- ✅ Код не возвращается в API

### Рекомендации для продакшена:
1. **Rate limiting:**
   - Максимум 3 запроса на сброс в час
   - Cooldown 60 секунд между запросами

2. **CAPTCHA:**
   - Добавить reCAPTCHA на форму запроса

3. **Логирование:**
   - Записывать все попытки сброса
   - Мониторинг подозрительной активности

4. **Уведомления:**
   - Email при успешном сбросе пароля
   - Email при создании запроса администрации

## 📁 Созданные файлы

1. **Backend:**
   - `server/utils/emailService.js` - добавлена функция `sendPasswordResetCode()`
   - `server/routes/auth.js` - добавлены 3 новых endpoint
   - `add-password-reset-table.sql` - SQL миграция

2. **Frontend:**
   - `src/pages/ForgotPassword.jsx` - страница сброса пароля
   - `src/pages/Auth.css` - стили для страницы сброса
   - `src/App.jsx` - добавлен маршрут `/forgot-password`

3. **Обновлённые файлы:**
   - `src/pages/StudentAuth.jsx` - добавлена ссылка "Забыли пароль?"
   - `src/pages/ChefAuth.jsx` - добавлена ссылка "Забыли пароль?"
   - `src/pages/AdminAuth.jsx` - добавлена ссылка "Забыли пароль?"

## 📊 Статус

| Компонент | Статус |
|-----------|--------|
| Email сброс | ✅ Готов |
| Админ запросы | ✅ Готов |
| База данных | ✅ Создана |
| UI/UX | ✅ Готов |
| Email шаблон | ✅ Готов |
| Безопасность | ✅ Реализована |
| Тестирование | 🧪 Готов к тесту |

## 🚀 Готово к использованию!

Система сброса пароля полностью настроена и готова к тестированию.

**Протестируйте прямо сейчас:**
1. Откройте http://localhost:5000
2. Нажмите "Забыли пароль?" на любой странице входа
3. Выберите метод восстановления
4. Следуйте инструкциям

---

**Дата:** 27 января 2026, 12:40  
**Версия:** 3.0.0  
**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ
