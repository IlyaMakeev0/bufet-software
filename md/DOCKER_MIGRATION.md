# 🐳 Docker Migration Summary

## Что было сделано

Проект успешно контейнеризирован с использованием Docker. Архитектура разделена на два контейнера:
1. **База данных** (PostgreSQL)
2. **Приложение** (React Frontend + Express Backend)

---

## 📁 Созданные файлы

### Основные конфигурационные файлы
- `Dockerfile` - конфигурация образа приложения (multi-stage build)
- `docker-compose.yml` - оркестрация контейнеров
- `.dockerignore` - исключения при сборке образа
- `.env.docker` - переменные окружения для Docker
- `init-db.sql` - скрипт инициализации PostgreSQL

### Адаптеры базы данных
- `server/database-postgres.js` - адаптер для PostgreSQL (для Docker)
- `server/index-docker.js` - точка входа с поддержкой обеих БД

### Документация
- `DOCKER_GUIDE.md` - подробное руководство (60+ страниц)
- `DOCKER_QUICKSTART.md` - быстрый старт
- `DOCKER_README.md` - обзор архитектуры
- `DOCKER_MIGRATION.md` - этот файл

### Windows скрипты (bat-файлы)
- `docker-start.bat` - запуск контейнеров
- `docker-stop.bat` - остановка контейнеров
- `docker-rebuild.bat` - пересборка образов
- `docker-logs.bat` - просмотр логов
- `docker-test.bat` - проверка готовности к запуску

### Обновленные файлы
- `package.json` - добавлена зависимость `pg` (PostgreSQL driver)
- `.gitignore` - добавлены Docker-специфичные исключения

---

## 🏗️ Архитектура

### Контейнер 1: База данных
```yaml
Образ: postgres:15-alpine
Порт: 5432
Volume: postgres_data (постоянное хранилище)
Credentials:
  - Database: school_canteen
  - User: canteen_user
  - Password: canteen_password_2024
```

### Контейнер 2: Приложение
```yaml
Образ: собирается из Dockerfile
Порт: 5000
Компоненты:
  - React Frontend (Vite build)
  - Express Backend
  - API endpoints
```

### Сеть
```yaml
Network: canteen-network (bridge)
Связь: app → db:5432
```

---

## 🚀 Как запустить

### Вариант 1: Windows (двойной клик)
1. Запустите `docker-test.bat` - проверка готовности
2. Запустите `docker-start.bat` - запуск проекта
3. Откройте http://localhost:5000

### Вариант 2: Командная строка
```bash
# Проверка
docker --version
docker-compose --version

# Запуск
docker-compose up --build -d

# Проверка статуса
docker ps

# Открыть в браузере
start http://localhost:5000
```

### Вариант 3: Linux/Mac
```bash
# Запуск
docker-compose up --build -d

# Проверка
docker ps
curl http://localhost:5000/health

# Открыть в браузере
open http://localhost:5000  # Mac
xdg-open http://localhost:5000  # Linux
```

---

## ✅ Проверка работы

### 1. Проверка контейнеров
```bash
docker ps
```
Должны быть запущены:
- `school-canteen-app` (порт 5000)
- `school-canteen-db` (порт 5432)

### 2. Health Check
```bash
curl http://localhost:5000/health
```
Ожидаемый ответ:
```json
{"status":"ok","timestamp":"2024-01-24T..."}
```

### 3. Проверка базы данных
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen
```
Внутри psql:
```sql
\dt                          -- Список таблиц
SELECT COUNT(*) FROM menu;   -- Должно быть ~270 записей
\q                           -- Выход
```

### 4. Проверка в браузере
1. Откройте http://localhost:5000
2. Главная страница должна загрузиться
3. Попробуйте зарегистрироваться
4. Попробуйте войти

---

## 🎮 Управление контейнерами

### Основные команды

| Действие | Windows | Linux/Mac |
|----------|---------|-----------|
| Запуск | `docker-start.bat` | `docker-compose up -d` |
| Остановка | `docker-stop.bat` | `docker-compose down` |
| Пересборка | `docker-rebuild.bat` | `docker-compose up --build -d` |
| Логи | `docker-logs.bat` | `docker-compose logs -f` |
| Статус | `docker ps` | `docker ps` |

### Дополнительные команды
```bash
# Перезапуск
docker-compose restart

# Остановка с удалением данных
docker-compose down -v

# Просмотр логов конкретного сервиса
docker-compose logs app
docker-compose logs db

# Выполнение команды в контейнере
docker exec -it school-canteen-app sh
docker exec -it school-canteen-db sh
```

---

## 🔄 Миграция данных

### Из SQLite в PostgreSQL

Если у вас есть существующая база SQLite (`school_canteen.db`), данные будут автоматически пересозданы в PostgreSQL при первом запуске.

### Backup и восстановление

#### Создание backup
```bash
docker exec school-canteen-db pg_dump -U canteen_user school_canteen > backup.sql
```

#### Восстановление из backup
```bash
docker exec -i school-canteen-db psql -U canteen_user school_canteen < backup.sql
```

---

## 🔧 Troubleshooting

### Проблема: Порт 5000 занят
**Решение 1**: Остановите приложение на порту 5000
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :5000
kill -9 <PID>
```

**Решение 2**: Измените порт в `docker-compose.yml`
```yaml
app:
  ports:
    - "5001:5000"  # Используйте 5001 вместо 5000
```

### Проблема: Docker не запускается
1. Убедитесь, что Docker Desktop запущен
2. Проверьте: `docker ps`
3. Перезапустите Docker Desktop

### Проблема: Контейнер падает
```bash
# Просмотр логов
docker-compose logs app

# Пересборка без кэша
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: База данных не инициализируется
```bash
# Удалить volumes и пересоздать
docker-compose down -v
docker-compose up --build -d
```

---

## 📊 Мониторинг

### Использование ресурсов
```bash
# Статистика в реальном времени
docker stats

# Дисковое пространство
docker system df
```

### Логи
```bash
# Все логи
docker-compose logs

# Последние 100 строк
docker-compose logs --tail=100

# Логи за последний час
docker-compose logs --since 1h

# Экспорт в файл
docker-compose logs > logs.txt
```

---

## 🌐 Production Deployment

### Рекомендации

1. **Безопасность**
   - Измените пароли в `.env.docker`
   - Используйте Docker Secrets для production
   - Настройте HTTPS

2. **Reverse Proxy**
   - Используйте nginx или Traefik
   - Настройте SSL/TLS сертификаты

3. **Backup**
   - Настройте автоматические backup БД
   - Используйте cron для регулярных backup

4. **Мониторинг**
   - Prometheus + Grafana
   - Логирование (ELK Stack)
   - Алертинг

5. **Масштабирование**
   - Используйте Docker Swarm или Kubernetes
   - Настройте load balancing

---

## 📚 Документация

### Основные документы
- **DOCKER_QUICKSTART.md** - быстрый старт (5 минут)
- **DOCKER_GUIDE.md** - полное руководство (все детали)
- **DOCKER_README.md** - обзор архитектуры

### Внешние ресурсы
- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [PostgreSQL Docker Image](https://hub.docker.com/_/postgres)

---

## 🎯 Преимущества Docker-архитектуры

### Для разработки
- ✅ Одинаковое окружение для всех разработчиков
- ✅ Быстрый старт новых разработчиков
- ✅ Изоляция от хост-системы
- ✅ Легкое переключение между версиями

### Для production
- ✅ Воспроизводимые деплои
- ✅ Легкое масштабирование
- ✅ Изоляция сервисов
- ✅ Простой откат к предыдущей версии

### Для DevOps
- ✅ CI/CD интеграция
- ✅ Автоматизация деплоя
- ✅ Мониторинг и логирование
- ✅ Управление ресурсами

---

## 📝 Чеклист после миграции

- [x] Dockerfile создан
- [x] docker-compose.yml настроен
- [x] PostgreSQL адаптер реализован
- [x] Документация написана
- [x] Windows скрипты созданы
- [x] .gitignore обновлен
- [x] package.json обновлен
- [ ] Docker установлен
- [ ] Проект запущен в Docker
- [ ] Все функции протестированы
- [ ] Production конфигурация настроена

---

## 🎉 Готово!

Проект успешно контейнеризирован и готов к использованию!

**Следующие шаги:**
1. Запустите `docker-test.bat` для проверки
2. Запустите `docker-start.bat` для старта
3. Откройте http://localhost:5000
4. Протестируйте все функции
5. Прочитайте DOCKER_GUIDE.md для деталей

**Вопросы?** См. раздел Troubleshooting в DOCKER_GUIDE.md
