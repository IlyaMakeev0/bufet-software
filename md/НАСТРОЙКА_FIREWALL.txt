╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🔧 НАСТРОЙКА FIREWALL ДЛЯ ДОСТУПА ИЗВНЕ               ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ПРОБЛЕМА:                                               │
└───────────────────────────────────────────────────────────┘

На localhost работает, но извне (autogreatfood.ru) не доступен.

Причина: Firewall блокирует порты 8080 и 8443.

┌───────────────────────────────────────────────────────────┐
│  🚀 РЕШЕНИЕ - ОТКРОЙТЕ ПОРТЫ:                            │
└───────────────────────────────────────────────────────────┘

Выполните на сервере:

# 1. Проверьте статус firewall
sudo ufw status

# 2. Откройте порты 8080 и 8443
sudo ufw allow 8080/tcp
sudo ufw allow 8443/tcp

# 3. Если используете firewalld (CentOS/RHEL):
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --permanent --add-port=8443/tcp
sudo firewall-cmd --reload

# 4. Проверьте, что порты открыты
sudo ufw status | grep -E '(8080|8443)'

┌───────────────────────────────────────────────────────────┐
│  🔍 ДИАГНОСТИКА:                                         │
└───────────────────────────────────────────────────────────┘

# 1. Проверьте, что Docker слушает на всех интерфейсах
netstat -tlnp | grep -E ':(8080|8443)'

Должно быть:
   0.0.0.0:8080
   0.0.0.0:8443

# 2. Проверьте логи backend
docker logs school-canteen-backend | grep "Server running"

# 3. Попробуйте подключиться с сервера
curl -k https://localhost:8443/health

# 4. Проверьте DNS
nslookup autogreatfood.ru

# 5. Проверьте, что домен указывает на ваш сервер
curl -I https://autogreatfood.ru

┌───────────────────────────────────────────────────────────┐
│  ⚠️  ВАЖНО ДЛЯ HTTPS:                                    │
└───────────────────────────────────────────────────────────┘

Если вы используете Nginx или другой reverse proxy перед Docker:

1. Настройте Nginx для проксирования на порты 8080/8443

2. Или используйте стандартные порты 80/443 в docker-compose:

   В docker-compose.production.yml измените:
   
   ports:
     - "80:8080"    # Внешний 80 → внутренний 8080
     - "443:8443"   # Внешний 443 → внутренний 8443

   НО ПОМНИТЕ: порты 443 и 1593 у вас заняты!

┌───────────────────────────────────────────────────────────┐
│  🌐 ВАРИАНТЫ ДОСТУПА:                                    │
└───────────────────────────────────────────────────────────┘

ВАРИАНТ 1 - Прямой доступ к Docker:
   https://autogreatfood.ru:8443
   http://autogreatfood.ru:8080

ВАРИАНТ 2 - Через Nginx reverse proxy:
   https://autogreatfood.ru (Nginx → Docker 8443)

ВАРИАНТ 3 - Изменить порты Docker на стандартные:
   (Но у вас 443 занят!)

┌───────────────────────────────────────────────────────────┐
│  📋 КОНФИГУРАЦИЯ NGINX (если используете):               │
└───────────────────────────────────────────────────────────┘

Создайте файл /etc/nginx/sites-available/autogreatfood.ru:

server {
    listen 80;
    server_name autogreatfood.ru www.autogreatfood.ru;
    
    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 8444 ssl http2;  # Используем 8444 вместо 443
    server_name autogreatfood.ru www.autogreatfood.ru;
    
    ssl_certificate /path/to/cert/www_autogreatfood_ru_2026_08_30.crt;
    ssl_certificate_key /path/to/cert/key.txt;
    
    location / {
        proxy_pass https://localhost:8443;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_ssl_verify off;
    }
}

Затем:
sudo ln -s /etc/nginx/sites-available/autogreatfood.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

И откройте порт 8444:
sudo ufw allow 8444/tcp

┌───────────────────────────────────────────────────────────┐
│  🔧 БЫСТРОЕ РЕШЕНИЕ:                                     │
└───────────────────────────────────────────────────────────┘

Если нужен быстрый доступ:

# 1. Откройте порты
sudo ufw allow 8080/tcp
sudo ufw allow 8443/tcp

# 2. Проверьте доступ
curl -k https://autogreatfood.ru:8443/health

# 3. Откройте в браузере
https://autogreatfood.ru:8443

┌───────────────────────────────────────────────────────────┐
│  📊 ПРОВЕРКА:                                            │
└───────────────────────────────────────────────────────────┘

# С сервера
curl -k https://localhost:8443/health

# Извне (с вашего компьютера)
curl -k https://autogreatfood.ru:8443/health

# В браузере
https://autogreatfood.ru:8443

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ ОТКРОЙТЕ ПОРТЫ 8080 И 8443 В FIREWALL              ║
║                                                           ║
║   sudo ufw allow 8080/tcp                                ║
║   sudo ufw allow 8443/tcp                                ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
