# Руководство по хостингу

## Проблема с базой данных

При хостинге возникает ошибка базы данных из-за неправильной конфигурации. Вот решения:

### Вариант 1: Использование PostgreSQL (Рекомендуется)

1. **Создайте PostgreSQL базу данных** на вашем хостинге (например, на Heroku, Railway, Render)

2. **Настройте переменные окружения:**
   ```bash
   NODE_ENV=production
   DB_HOST=your-postgres-host.com
   DB_PORT=5432
   DB_NAME=school_canteen
   DB_USER=your_username
   DB_PASSWORD=your_password
   FRONTEND_URL=https://your-domain.com
   SESSION_SECRET=your-random-secret-key
   ```

3. **Деплой:**
   - Приложение автоматически создаст все таблицы при первом запуске
   - Используется файл `server/database.js` (PostgreSQL)

### Вариант 2: Использование SQLite (Только для тестирования)

1. **Удалите переменную DB_HOST** из окружения
2. Приложение автоматически переключится на SQLite
3. **НЕ РЕКОМЕНДУЕТСЯ** для production!

### Популярные платформы для хостинга

#### Railway.app (Рекомендуется)
```bash
# 1. Установите Railway CLI
npm install -g @railway/cli

# 2. Войдите
railway login

# 3. Создайте проект
railway init

# 4. Добавьте PostgreSQL
railway add postgresql

# 5. Деплой
railway up
```

#### Render.com
1. Создайте Web Service
2. Добавьте PostgreSQL database
3. Настройте переменные окружения
4. Deploy

#### Heroku
```bash
# 1. Создайте приложение
heroku create your-app-name

# 2. Добавьте PostgreSQL
heroku addons:create heroku-postgresql:mini

# 3. Деплой
git push heroku main
```

### Проверка подключения к БД

После деплоя проверьте логи:
```bash
# Railway
railway logs

# Render
# Смотрите в Dashboard -> Logs

# Heroku
heroku logs --tail
```

Вы должны увидеть:
```
✅ Database initialized
🚀 Server running on: http://0.0.0.0:5000
```

### Частые ошибки

1. **"Connection refused"**
   - Проверьте DB_HOST, DB_PORT
   - Убедитесь, что база данных запущена

2. **"Authentication failed"**
   - Проверьте DB_USER и DB_PASSWORD
   - Убедитесь, что пользователь имеет права

3. **"Database does not exist"**
   - Создайте базу данных вручную
   - Или используйте автосоздание (зависит от хостинга)

### Миграция данных

Если у вас уже есть данные в SQLite:
```bash
# Экспорт из SQLite
sqlite3 school_canteen.db .dump > backup.sql

# Импорт в PostgreSQL (адаптируйте синтаксис)
psql -h your-host -U your-user -d school_canteen < backup_adapted.sql
```

### Безопасность

1. **Измените SESSION_SECRET** на случайную строку
2. **Используйте сильные пароли** для БД
3. **Включите HTTPS** на хостинге
4. **Ограничьте доступ к БД** только с IP вашего сервера

### Мониторинг

После деплоя проверьте:
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Создание заказов работает
- [ ] Пополнение баланса работает
- [ ] Данные сохраняются после перезапуска

## Быстрый старт с Docker

Если ваш хостинг поддерживает Docker:

```bash
# Билд
docker-compose build

# Запуск
docker-compose up -d

# Проверка
docker-compose logs -f
```

## Поддержка

Если проблемы остаются:
1. Проверьте логи сервера
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к БД отдельно
