# 🌐 Решение проблемы email на сервере 89.169.52.76

## 🔍 Проблема

На локальном компьютере email работает, но на сервере 89.169.52.76 зависает.

**Причина:** На большинстве VPS/хостингов **заблокированы порты 25, 465, 587** для предотвращения спама.

---

## ✅ Решения

### Решение 1: Используйте внешний Email API сервис (рекомендуется) ⭐

Вместо прямого SMTP используйте специализированные сервисы:

#### A) SendGrid (бесплатно 100 писем/день)

1. **Регистрация:**
   - https://signup.sendgrid.com/
   - Подтвердите email
   - Создайте API ключ

2. **Установите пакет:**
   ```bash
   npm install @sendgrid/mail
   ```

3. **Обновите `server/utils/emailService.js`:**
   ```javascript
   import sgMail from '@sendgrid/mail'
   
   sgMail.setApiKey(process.env.SENDGRID_API_KEY)
   
   export async function sendVerificationCode(email, code) {
     const msg = {
       to: email,
       from: 'noreply@yourdomain.com', // Ваш верифицированный домен
       subject: 'Код подтверждения регистрации',
       text: `Ваш код подтверждения: ${code}`,
       html: `<strong>Ваш код подтверждения: ${code}</strong>`
     }
     
     try {
       await sgMail.send(msg)
       console.log('✅ Email sent via SendGrid')
       return { success: true }
     } catch (error) {
       console.error('❌ SendGrid error:', error)
       throw error
     }
   }
   ```

4. **Добавьте в `.env` на сервере:**
   ```env
   SENDGRID_API_KEY=SG.ваш-api-ключ
   ```

#### B) Mailgun (бесплатно 5000 писем/месяц)

1. **Регистрация:**
   - https://signup.mailgun.com/
   - Подтвердите домен

2. **Установите пакет:**
   ```bash
   npm install mailgun.js form-data
   ```

3. **Обновите `server/utils/emailService.js`:**
   ```javascript
   import formData from 'form-data'
   import Mailgun from 'mailgun.js'
   
   const mailgun = new Mailgun(formData)
   const mg = mailgun.client({
     username: 'api',
     key: process.env.MAILGUN_API_KEY
   })
   
   export async function sendVerificationCode(email, code) {
     try {
       await mg.messages.create(process.env.MAILGUN_DOMAIN, {
         from: 'Школьная столовая <noreply@yourdomain.com>',
         to: [email],
         subject: 'Код подтверждения регистрации',
         text: `Ваш код подтверждения: ${code}`,
         html: `<strong>Ваш код подтверждения: ${code}</strong>`
       })
       console.log('✅ Email sent via Mailgun')
       return { success: true }
     } catch (error) {
       console.error('❌ Mailgun error:', error)
       throw error
     }
   }
   ```

#### C) Resend (современный, простой)

1. **Регистрация:**
   - https://resend.com/
   - Получите API ключ

2. **Установите пакет:**
   ```bash
   npm install resend
   ```

3. **Обновите `server/utils/emailService.js`:**
   ```javascript
   import { Resend } from 'resend'
   
   const resend = new Resend(process.env.RESEND_API_KEY)
   
   export async function sendVerificationCode(email, code) {
     try {
       await resend.emails.send({
         from: 'Школьная столовая <noreply@yourdomain.com>',
         to: email,
         subject: 'Код подтверждения регистрации',
         html: `<strong>Ваш код подтверждения: ${code}</strong>`
       })
       console.log('✅ Email sent via Resend')
       return { success: true }
     } catch (error) {
       console.error('❌ Resend error:', error)
       throw error
     }
   }
   ```

---

### Решение 2: Используйте порт 465 (SSL) вместо 587

Иногда порт 465 не заблокирован:

```javascript
// server/utils/emailService.js
const SMTP_CONFIG = {
  host: 'smtp.gmail.com',
  port: 465,              // ← Изменено с 587
  secure: true,           // ← Изменено с false
  auth: {
    user: 'ppredprof@gmail.com',
    pass: 'xvzr khqt hckc wabb'
  }
}
```

**Проверка:**
```bash
# На сервере выполните:
telnet smtp.gmail.com 465
# Если подключается - порт открыт
```

---

### Решение 3: Используйте SMTP relay через сервер

Настройте локальный SMTP relay (Postfix) на сервере:

```bash
# На сервере (Ubuntu/Debian)
sudo apt update
sudo apt install postfix mailutils

# Выберите "Internet Site"
# Укажите ваш домен
```

Затем в коде:
```javascript
const SMTP_CONFIG = {
  host: 'localhost',
  port: 25,
  secure: false,
  auth: false  // Не нужна авторизация для localhost
}
```

---

### Решение 4: Отключите email верификацию (временно)

Для тестирования на сервере можно временно отключить:

```javascript
// server/routes/auth.js

// В функции register, закомментируйте проверку кода:
/*
if (role === 'student') {
  if (!verificationCode) {
    return res.status(400).json({ error: 'Введите код подтверждения' })
  }
  // ... проверка кода
}
*/

// Или автоматически верифицируйте всех:
// После создания пользователя:
await runQuery('UPDATE users SET email_verified = 1 WHERE id = ?', [userId])
```

---

## 🔍 Диагностика на сервере

### Проверка 1: Доступен ли порт 587?

```bash
# SSH на сервер
ssh root@89.169.52.76

# Проверка порта 587
telnet smtp.gmail.com 587

# Если зависает или "Connection refused" - порт заблокирован
```

### Проверка 2: Доступен ли порт 465?

```bash
telnet smtp.gmail.com 465

# Если подключается - используйте порт 465
```

### Проверка 3: Логи сервера

```bash
# Смотрите логи Node.js
pm2 logs

# Или если используете systemd
journalctl -u your-app-name -f
```

### Проверка 4: Firewall

```bash
# Проверьте правила firewall
sudo iptables -L -n

# Или ufw
sudo ufw status
```

---

## 📊 Сравнение решений

| Решение | Сложность | Надежность | Стоимость | Рекомендация |
|---------|-----------|------------|-----------|--------------|
| **SendGrid** | Легко | ⭐⭐⭐⭐⭐ | Бесплатно (100/день) | ✅ Лучший выбор |
| **Mailgun** | Легко | ⭐⭐⭐⭐⭐ | Бесплатно (5000/мес) | ✅ Отлично |
| **Resend** | Легко | ⭐⭐⭐⭐⭐ | Бесплатно (3000/мес) | ✅ Современный |
| **Порт 465** | Легко | ⭐⭐⭐ | Бесплатно | ⚠️ Может не работать |
| **SMTP Relay** | Сложно | ⭐⭐⭐⭐ | Бесплатно | ⚠️ Требует настройки |
| **Без верификации** | Легко | ⭐ | Бесплатно | ❌ Только для теста |

---

## 🚀 Быстрое решение (SendGrid)

### Шаг 1: Регистрация (5 минут)

1. https://signup.sendgrid.com/
2. Подтвердите email
3. Settings → API Keys → Create API Key
4. Скопируйте ключ (начинается с `SG.`)

### Шаг 2: Установка на сервере

```bash
# SSH на сервер
ssh root@89.169.52.76

# Перейдите в папку проекта
cd /path/to/your/project

# Установите SendGrid
npm install @sendgrid/mail

# Добавьте в .env
echo "SENDGRID_API_KEY=SG.ваш-ключ" >> .env
```

### Шаг 3: Обновите код

Создайте новый файл `server/utils/emailService-sendgrid.js`:

```javascript
import sgMail from '@sendgrid/mail'

sgMail.setApiKey(process.env.SENDGRID_API_KEY)

export async function sendVerificationCode(email, code) {
  console.log(`📧 Отправка через SendGrid на ${email}`)
  
  const msg = {
    to: email,
    from: 'ppredprof@gmail.com', // Должен быть верифицирован в SendGrid
    subject: 'Код подтверждения регистрации',
    text: `Ваш код подтверждения: ${code}\n\nКод действителен 10 минут.`,
    html: `
      <div style="font-family: Arial, sans-serif; padding: 20px;">
        <h2>Код подтверждения регистрации</h2>
        <p>Ваш код подтверждения:</p>
        <h1 style="color: #1565c0; font-size: 36px; letter-spacing: 8px;">${code}</h1>
        <p>Код действителен в течение 10 минут.</p>
        <p style="color: #666; font-size: 12px;">© 2026 Школьная столовая</p>
      </div>
    `
  }
  
  try {
    const response = await sgMail.send(msg)
    console.log('✅ Email sent via SendGrid:', response[0].statusCode)
    return { success: true }
  } catch (error) {
    console.error('❌ SendGrid error:', error.response?.body || error)
    throw error
  }
}

export async function sendPasswordResetCode(email, code) {
  // Аналогично
}
```

### Шаг 4: Обновите импорт

В `server/routes/auth.js`:

```javascript
// Замените:
// import { sendVerificationCode } from '../utils/emailService.js'

// На:
import { sendVerificationCode } from '../utils/emailService-sendgrid.js'
```

### Шаг 5: Перезапустите

```bash
# На сервере
pm2 restart all

# Или
npm run server
```

---

## ✅ Проверка работы

### На сервере:

```bash
# Смотрите логи
pm2 logs

# Или
tail -f /path/to/logs/app.log
```

### В браузере:

1. Откройте http://89.169.52.76/student/register
2. Введите email
3. Нажмите "Получить код"
4. Проверьте почту

---

## 🎯 Рекомендация

**Для production сервера используйте SendGrid или Mailgun!**

Преимущества:
- ✅ Работает на любом хостинге
- ✅ Не зависит от портов
- ✅ Высокая доставляемость
- ✅ Статистика отправок
- ✅ Бесплатный план

**Не используйте прямой SMTP на production серверах!**

---

## 📝 Чеклист для сервера

- [ ] Проверили доступность портов 587/465
- [ ] Зарегистрировались в SendGrid/Mailgun
- [ ] Получили API ключ
- [ ] Установили пакет на сервере
- [ ] Обновили код
- [ ] Добавили API ключ в .env
- [ ] Перезапустили сервер
- [ ] Протестировали отправку

---

## 📞 Нужна помощь?

Пришлите:
1. Результат `telnet smtp.gmail.com 587` на сервере
2. Результат `telnet smtp.gmail.com 465` на сервере
3. Логи сервера при попытке отправки

Я помогу настроить!

---

**Дата:** 28 января 2026  
**Сервер:** 89.169.52.76  
**Статус:** Порты SMTP скорее всего заблокированы  
**Решение:** Используйте SendGrid/Mailgun
