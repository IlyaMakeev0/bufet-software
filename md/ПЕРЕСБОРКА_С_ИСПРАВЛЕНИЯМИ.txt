╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ ИСПРАВЛЕНО! ПЕРЕСОБЕРИТЕ КОНТЕЙНЕРЫ                 ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ЧТО БЫЛО ИСПРАВЛЕНО:                                    │
└───────────────────────────────────────────────────────────┘

✅ Добавлена обработка ошибок при загрузке SSL сертификатов
✅ Сервер теперь работает даже если сертификаты не найдены
✅ Healthcheck проверяет и HTTPS, и HTTP
✅ Исправлены права доступа к папке cert
✅ Улучшено логирование для диагностики

┌───────────────────────────────────────────────────────────┐
│  🚀 КОМАНДЫ ДЛЯ ПЕРЕСБОРКИ:                              │
└───────────────────────────────────────────────────────────┘

Выполните на сервере:

1️⃣  Остановите контейнеры:

    docker-compose -f docker-compose.production.yml down


2️⃣  Пересоберите backend:

    docker-compose -f docker-compose.production.yml build --no-cache backend


3️⃣  Запустите:

    docker-compose -f docker-compose.production.yml up -d


4️⃣  Смотрите логи в реальном времени:

    docker logs school-canteen-backend -f

    (Нажмите Ctrl+C для выхода)

┌───────────────────────────────────────────────────────────┐
│  🔍 ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ:                             │
└───────────────────────────────────────────────────────────┘

ВАРИАНТ 1 - С SSL (если сертификаты найдены):

   ✅ SSL certificates loaded successfully
   ⏳ Waiting for PostgreSQL...
   ✅ Database initialized
   🔓 HTTP Server (redirect) running on port 8080
   🚀 HTTPS Server running on port 8443
   🔒 SSL: Enabled
   📜 Certificate: www_autogreatfood_ru_2026_08_30.crt

ВАРИАНТ 2 - БЕЗ SSL (если сертификаты не найдены):

   ⚠️  SSL certificates not found, will run HTTP only
      Key: /app/cert/key.txt
      Cert: /app/cert/www_autogreatfood_ru_2026_08_30.crt
   ⏳ Waiting for PostgreSQL...
   ✅ Database initialized
   🚀 HTTP Server running on port 8080
   ⚠️  SSL: Disabled (certificates not found)

┌───────────────────────────────────────────────────────────┐
│  🌐 ДОСТУП К ПРИЛОЖЕНИЮ:                                 │
└───────────────────────────────────────────────────────────┘

Если SSL работает:
   https://localhost:8443
   https://autogreatfood.ru

Если SSL не работает (временно):
   http://localhost:8080

Health Check:
   curl -k https://localhost:8443/health
   или
   curl http://localhost:8080/health

┌───────────────────────────────────────────────────────────┐
│  🔧 ЕСЛИ СЕРТИФИКАТЫ НЕ НАЙДЕНЫ:                         │
└───────────────────────────────────────────────────────────┘

1️⃣  Проверьте, что сертификаты есть на хосте:

    ls -la cert/
    
    Должно быть:
    -rw-r--r-- 1 user user 1707 ... key.txt
    -rw-r--r-- 1 user user 2277 ... www_autogreatfood_ru_2026_08_30.crt


2️⃣  Проверьте, что они монтируются в контейнер:

    docker exec school-canteen-backend ls -la /app/cert/


3️⃣  Если в контейнере пусто, проверьте docker-compose.production.yml:

    grep -A 2 "volumes:" docker-compose.production.yml
    
    Должно быть:
    volumes:
      - ./cert:/app/cert:ro


4️⃣  Проверьте абсолютный путь:

    pwd
    ls -la $(pwd)/cert/


5️⃣  Если нужно, исправьте путь в docker-compose.production.yml:

    volumes:
      - /полный/путь/к/cert:/app/cert:ro


6️⃣  Перезапустите:

    docker-compose -f docker-compose.production.yml restart backend

┌───────────────────────────────────────────────────────────┐
│  📊 ПРОВЕРКА HEALTHCHECK:                                │
└───────────────────────────────────────────────────────────┘

Проверьте статус healthcheck:

    docker inspect school-canteen-backend --format='{{.State.Health.Status}}'

Должно быть: healthy

Если unhealthy, посмотрите детали:

    docker inspect school-canteen-backend --format='{{json .State.Health}}' | python3 -m json.tool

┌───────────────────────────────────────────────────────────┐
│  🛠️  ПОЛЕЗНЫЕ КОМАНДЫ:                                   │
└───────────────────────────────────────────────────────────┘

Посмотреть статус:
    docker ps

Посмотреть логи PostgreSQL:
    docker logs school-canteen-db

Зайти в контейнер:
    docker exec -it school-canteen-backend sh

Проверить сертификаты в контейнере:
    docker exec school-canteen-backend ls -la /app/cert/

Проверить переменные окружения:
    docker exec school-canteen-backend env | grep PORT

Перезапустить только backend:
    docker-compose -f docker-compose.production.yml restart backend

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🎉 ТЕПЕРЬ СЕРВЕР ДОЛЖЕН ЗАПУСТИТЬСЯ                    ║
║                                                           ║
║   Даже если сертификаты не найдены, он будет работать   ║
║   на HTTP порту 8080                                     ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
