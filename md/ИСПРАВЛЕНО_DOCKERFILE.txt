╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ DOCKERFILE ИСПРАВЛЕН                                ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ПРОБЛЕМА:                                               │
└───────────────────────────────────────────────────────────┘

chown: unknown user/group nodejs:nodejs

Пользователь nodejs не был создан на момент выполнения chown.

┌───────────────────────────────────────────────────────────┐
│  РЕШЕНИЕ:                                                │
└───────────────────────────────────────────────────────────┘

Изменен порядок команд в Dockerfile:
1. Сначала создается пользователь nodejs
2. Потом создается папка cert и меняются права

┌───────────────────────────────────────────────────────────┐
│  🚀 КОМАНДЫ ДЛЯ ЗАПУСКА:                                 │
└───────────────────────────────────────────────────────────┘

Выполните на сервере:

docker-compose -f docker-compose.production.yml build --no-cache backend

docker-compose -f docker-compose.production.yml up -d

docker logs school-canteen-backend -f

┌───────────────────────────────────────────────────────────┐
│  🔍 ЧТО ОЖИДАТЬ:                                         │
└───────────────────────────────────────────────────────────┘

Сборка должна пройти успешно:

[+] Building ...
=> [builder 1/7] FROM node:18-alpine
=> [builder 7/7] RUN npm run build
=> [stage-1 1/8] FROM node:18-alpine
=> [stage-1 8/8] RUN mkdir -p /app/cert && chown -R nodejs:nodejs /app
=> => naming to docker.io/library/bufet-software-backend

Затем в логах:

✅ SSL certificates loaded successfully (или warning если нет)
⏳ Waiting for PostgreSQL...
✅ Database initialized
🚀 HTTPS Server running on port 8443 (или HTTP на 8080)

┌───────────────────────────────────────────────────────────┐
│  🌐 ДОСТУП:                                              │
└───────────────────────────────────────────────────────────┘

https://localhost:8443
или
http://localhost:8080

Проверка:
curl -k https://localhost:8443/health

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ ТЕПЕРЬ ДОЛЖНО СОБРАТЬСЯ БЕЗ ОШИБОК                  ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
