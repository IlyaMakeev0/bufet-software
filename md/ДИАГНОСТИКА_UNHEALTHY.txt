╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🔍 ДИАГНОСТИКА ОШИБКИ "Container is unhealthy"        ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ПРОБЛЕМА:                                               │
└───────────────────────────────────────────────────────────┘

ERROR: for backend  Container "7d2000cc18e0" is unhealthy.

Контейнер запустился, но healthcheck не проходит.
Нужно посмотреть логи, чтобы понять причину.

┌───────────────────────────────────────────────────────────┐
│  🔍 КОМАНДЫ ДЛЯ ДИАГНОСТИКИ:                             │
└───────────────────────────────────────────────────────────┘

1️⃣  Посмотрите логи backend:

    docker logs school-canteen-backend


2️⃣  Посмотрите логи PostgreSQL:

    docker logs school-canteen-db


3️⃣  Проверьте статус контейнеров:

    docker ps -a


4️⃣  Проверьте healthcheck:

    docker inspect school-canteen-backend | grep -A 10 Health


5️⃣  Попробуйте зайти в контейнер:

    docker exec -it school-canteen-backend sh


6️⃣  Проверьте, есть ли сертификаты в контейнере:

    docker exec school-canteen-backend ls -la /app/cert/

┌───────────────────────────────────────────────────────────┐
│  🔧 ВОЗМОЖНЫЕ ПРИЧИНЫ И РЕШЕНИЯ:                         │
└───────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════
ПРИЧИНА 1: Сертификаты не найдены
═══════════════════════════════════════════════════════════

В логах будет:
   Error: ENOENT: no such file or directory, open '/app/cert/key.txt'

РЕШЕНИЕ:
   Проверьте, что сертификаты монтируются:
   
   docker exec school-canteen-backend ls -la /app/cert/
   
   Если пусто, проверьте docker-compose.production.yml:
   volumes:
     - ./cert:/app/cert:ro

───────────────────────────────────────────────────────────
ПРИЧИНА 2: База данных не готова
───────────────────────────────────────────────────────────

В логах будет:
   ⏳ Waiting for PostgreSQL...
   Error: connect ECONNREFUSED

РЕШЕНИЕ:
   Подождите 30-60 секунд и проверьте снова:
   
   docker logs school-canteen-db
   docker logs school-canteen-backend

───────────────────────────────────────────────────────────
ПРИЧИНА 3: Ошибка в коде сервера
───────────────────────────────────────────────────────────

В логах будет:
   SyntaxError: ...
   или
   Error: Cannot find module ...

РЕШЕНИЕ:
   Посмотрите полные логи:
   
   docker logs school-canteen-backend

───────────────────────────────────────────────────────────
ПРИЧИНА 4: Порты заняты
───────────────────────────────────────────────────────────

В логах будет:
   Error: listen EADDRINUSE: address already in use :::8443

РЕШЕНИЕ:
   Проверьте, что порты свободны на хосте:
   
   netstat -tlnp | grep -E ':(8080|8443)'
   
   Если заняты, остановите процесс или измените порты

───────────────────────────────────────────────────────────
ПРИЧИНА 5: Healthcheck не может подключиться
───────────────────────────────────────────────────────────

Healthcheck пытается подключиться к:
   https://localhost:8443/health

Но сервер может слушать на другом порту или не запуститься.

РЕШЕНИЕ:
   Проверьте, что сервер запустился:
   
   docker logs school-canteen-backend | grep "HTTPS Server"
   
   Должно быть:
   🚀 HTTPS Server running on port 8443

┌───────────────────────────────────────────────────────────┐
│  🚀 ВРЕМЕННОЕ РЕШЕНИЕ - ЗАПУСК БЕЗ HEALTHCHECK:         │
└───────────────────────────────────────────────────────────┘

Если нужно быстро запустить, можно временно отключить
healthcheck:

1️⃣  Остановите контейнеры:

    docker-compose -f docker-compose.production.yml down


2️⃣  Запустите БЕЗ проверки здоровья:

    docker-compose -f docker-compose.production.yml up -d --no-deps backend


3️⃣  Проверьте логи:

    docker logs school-canteen-backend -f


4️⃣  Если сервер запустился, проверьте вручную:

    curl -k https://localhost:8443/health

┌───────────────────────────────────────────────────────────┐
│  📋 ПОШАГОВАЯ ДИАГНОСТИКА:                               │
└───────────────────────────────────────────────────────────┘

Выполните эти команды и пришлите результат:

# 1. Логи backend (последние 50 строк)
docker logs school-canteen-backend --tail 50

# 2. Логи PostgreSQL (последние 20 строк)
docker logs school-canteen-db --tail 20

# 3. Статус контейнеров
docker ps -a

# 4. Проверка сертификатов в контейнере
docker exec school-canteen-backend ls -la /app/cert/ 2>&1 || echo "Контейнер не запущен или нет доступа"

# 5. Проверка healthcheck
docker inspect school-canteen-backend --format='{{json .State.Health}}' | python3 -m json.tool 2>/dev/null || echo "Healthcheck не настроен"

┌───────────────────────────────────────────────────────────┐
│  🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ (если сертификаты не найдены):  │
└───────────────────────────────────────────────────────────┘

Если проблема в сертификатах:

1️⃣  Проверьте, что папка cert существует:

    ls -la cert/


2️⃣  Проверьте права доступа:

    chmod 644 cert/*


3️⃣  Проверьте путь в docker-compose.production.yml:

    grep -A 2 "volumes:" docker-compose.production.yml


4️⃣  Должно быть:

    volumes:
      - ./cert:/app/cert:ro


5️⃣  Перезапустите:

    docker-compose -f docker-compose.production.yml restart backend

┌───────────────────────────────────────────────────────────┐
│  📊 ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ (если всё ОК):              │
└───────────────────────────────────────────────────────────┘

Backend:
   ⏳ Waiting for PostgreSQL...
   ✅ Database initialized
   🚀 HTTPS Server running on port 8443
   🔒 SSL: Enabled
   📜 Certificate: www_autogreatfood_ru_2026_08_30.crt

PostgreSQL:
   database system is ready to accept connections

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   📤 ПРИШЛИТЕ РЕЗУЛЬТАТ КОМАНДЫ:                         ║
║                                                           ║
║   docker logs school-canteen-backend --tail 50           ║
║                                                           ║
║   Это поможет понять причину проблемы                    ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
