╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   ✅ ОШИБКА ИСПРАВЛЕНА! ЗАПУСКАЙТЕ ЗАНОВО                ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────┐
│  ЧТО БЫЛО ИСПРАВЛЕНО:                                    │
└───────────────────────────────────────────────────────────┘

✅ Исправлен путь к Dockerfile в docker-compose
   Было: CICD/Dockerfile.backend
   Стало: server/CICD/Dockerfile.backend

✅ Обновлен .dockerignore
   Убраны блокировки важных файлов

✅ Упрощен Dockerfile
   Копируются только необходимые файлы

┌───────────────────────────────────────────────────────────┐
│  🚀 КОМАНДЫ ДЛЯ ЗАПУСКА:                                 │
└───────────────────────────────────────────────────────────┘

Выполните эти команды на сервере:

1️⃣  Остановите старые контейнеры:

    docker-compose down


2️⃣  Очистите кеш Docker (важно!):

    docker system prune -f


3️⃣  Пересоберите с нуля:

    docker-compose -f docker-compose.production.yml build --no-cache


4️⃣  Запустите:

    docker-compose -f docker-compose.production.yml up -d


5️⃣  Проверьте логи:

    docker logs school-canteen-backend -f

    (Нажмите Ctrl+C для выхода)


6️⃣  Проверьте статус:

    docker ps

┌───────────────────────────────────────────────────────────┐
│  🔍 ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ:                             │
└───────────────────────────────────────────────────────────┘

При сборке:
   [+] Building ...
   => [builder 1/7] FROM node:18-alpine
   => [builder 2/7] RUN apk add --no-cache python3 make g++
   => [builder 3/7] WORKDIR /app
   => [builder 4/7] COPY package*.json ./
   => [builder 5/7] RUN npm ci
   => [builder 6/7] COPY src ./src
   => [builder 7/7] RUN npm run build
   => [stage-1 1/8] FROM node:18-alpine
   ...
   => => naming to docker.io/library/bufet-software-backend

При запуске:
   ✅ Database initialized
   🚀 HTTPS Server running on port 8443
   🔒 SSL: Enabled
   📜 Certificate: www_autogreatfood_ru_2026_08_30.crt

┌───────────────────────────────────────────────────────────┐
│  🌐 ДОСТУП:                                              │
└───────────────────────────────────────────────────────────┘

HTTPS (основной):
   https://localhost:8443
   https://autogreatfood.ru

HTTP (редирект):
   http://localhost:8080

Health Check:
   curl -k https://localhost:8443/health

┌───────────────────────────────────────────────────────────┐
│  ⚠️  ЕСЛИ СНОВА ОШИБКА:                                  │
└───────────────────────────────────────────────────────────┘

1. Проверьте, что вы в правильной директории:
   
   pwd
   
   Должно быть: /path/to/bufet-software


2. Проверьте, что файлы на месте:
   
   ls -la server/CICD/Dockerfile.backend
   ls -la cert/
   ls -la docker-compose.production.yml


3. Посмотрите полные логи ошибки:
   
   docker-compose -f docker-compose.production.yml build backend


4. Если ошибка "cannot find module":
   
   Убедитесь, что package.json на месте:
   ls -la package.json


5. Если ошибка с сертификатами:
   
   Проверьте права доступа:
   ls -la cert/
   chmod 644 cert/*

┌───────────────────────────────────────────────────────────┐
│  📊 СТРУКТУРА ПРОЕКТА:                                   │
└───────────────────────────────────────────────────────────┘

bufet-software/
├── cert/                          ← SSL сертификаты
│   ├── key.txt
│   └── www_autogreatfood_ru_2026_08_30.crt
├── server/
│   ├── CICD/
│   │   └── Dockerfile.backend    ← Dockerfile для сборки
│   ├── index.js                   ← Главный файл сервера
│   └── ...
├── src/                           ← Исходники фронтенда
├── package.json
├── vite.config.js
├── docker-compose.production.yml  ← Конфигурация Docker
└── .dockerignore                  ← Исключения для Docker

┌───────────────────────────────────────────────────────────┐
│  🛠️  ПОЛЕЗНЫЕ КОМАНДЫ:                                   │
└───────────────────────────────────────────────────────────┘

Посмотреть все контейнеры:
   docker ps -a

Удалить все остановленные контейнеры:
   docker container prune -f

Удалить все неиспользуемые образы:
   docker image prune -a -f

Полная очистка Docker:
   docker system prune -a -f --volumes

Посмотреть логи PostgreSQL:
   docker logs school-canteen-db

Зайти в контейнер:
   docker exec -it school-canteen-backend sh

╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🎉 ТЕПЕРЬ ДОЛЖНО РАБОТАТЬ!                             ║
║                                                           ║
║   Запускайте команды выше и проверяйте логи              ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
