# 🪟 Инструкция для Windows

## 🚀 Быстрый запуск

### Шаг 1: Установка зависимостей
```cmd
npm install
```

### Шаг 2: Запуск проекта

**Терминал 1 (PowerShell или CMD):**
```cmd
npm run server
```

**Терминал 2 (PowerShell или CMD):**
```cmd
npm run dev
```

### Шаг 3: Открыть браузер

Попробуйте любой адрес:
- http://localhost:3000
- http://127.0.0.1:3000
- http://0.0.0.0:3000

---

## 🔧 Альтернативные порты

### Если порт 3000 занят

```cmd
npm run dev:3001
```
Откройте: http://localhost:3001

Или:
```cmd
npm run dev:8080
```
Откройте: http://localhost:8080

### Если порт 5000 занят

```cmd
npm run server:5001
```

⚠️ Обновите `vite.config.js`:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:5001'
  }
}
```

---

## 🐛 Решение проблем Windows

### Проблема: localhost не работает

**Решение 1:** Используйте 127.0.0.1
```
http://127.0.0.1:3000
```

**Решение 2:** Проверьте hosts файл
```cmd
notepad C:\Windows\System32\drivers\etc\hosts
```

Должна быть строка:
```
127.0.0.1 localhost
```

---

### Проблема: Порт занят

**Найти процесс:**
```cmd
netstat -ano | findstr :3000
```

**Убить процесс:**
```cmd
taskkill /PID <номер_процесса> /F
```

Пример:
```cmd
netstat -ano | findstr :3000
# Вывод: TCP 0.0.0.0:3000 0.0.0.0:0 LISTENING 12345

taskkill /PID 12345 /F
```

---

### Проблема: Ошибка "cannot find module"

**Решение:**
```cmd
# Удалить node_modules
rmdir /s /q node_modules

# Переустановить
npm install
```

---

### Проблема: Ошибка прав доступа

**Решение:** Запустите PowerShell/CMD от имени администратора

1. Найдите PowerShell в меню Пуск
2. Правый клик → "Запуск от имени администратора"
3. Перейдите в папку проекта
4. Запустите команды

---

### Проблема: Firewall блокирует

**Решение:**

1. Откройте "Брандмауэр Windows"
2. "Разрешить приложение через брандмауэр"
3. Найдите Node.js
4. Разрешите для частных и публичных сетей

---

### Проблема: Антивирус блокирует

**Решение:**

Добавьте папку проекта в исключения антивируса:
- Windows Defender
- Kaspersky
- Avast
- И т.д.

---

## 📝 Команды Windows

### Проверка портов
```cmd
# Проверить порт 3000
netstat -ano | findstr :3000

# Проверить порт 5000
netstat -ano | findstr :5000

# Показать все занятые порты
netstat -ano
```

### Управление процессами
```cmd
# Список процессов Node.js
tasklist | findstr node

# Убить все процессы Node.js
taskkill /IM node.exe /F
```

### Очистка
```cmd
# Очистить кэш npm
npm cache clean --force

# Удалить node_modules
rmdir /s /q node_modules

# Удалить package-lock.json
del package-lock.json

# Переустановить всё
npm install
```

---

## 🎯 Рекомендуемые инструменты

### Терминалы
- **Windows Terminal** (рекомендуется)
- PowerShell
- CMD
- Git Bash

### Редакторы
- **VS Code** (рекомендуется)
- WebStorm
- Sublime Text

### Браузеры
- Chrome (рекомендуется)
- Edge
- Firefox

---

## 💡 Советы для Windows

1. **Используйте Windows Terminal** - современный и удобный
2. **Запускайте от администратора** если есть проблемы с правами
3. **Добавьте в исключения антивируса** папку проекта
4. **Используйте 127.0.0.1** если localhost не работает
5. **Закройте Skype** - он часто занимает порт 80 и 443

---

## 🔥 Быстрые команды

### Полная переустановка
```cmd
rmdir /s /q node_modules
del package-lock.json
npm cache clean --force
npm install
```

### Убить все Node процессы
```cmd
taskkill /IM node.exe /F
```

### Проверить версии
```cmd
node --version
npm --version
```

### Обновить npm
```cmd
npm install -g npm@latest
```

---

## 📱 Доступ с телефона (в локальной сети)

1. Узнайте IP адрес компьютера:
```cmd
ipconfig
```

Найдите "IPv4-адрес", например: `192.168.1.100`

2. Откройте на телефоне:
```
http://192.168.1.100:3000
```

⚠️ Компьютер и телефон должны быть в одной Wi-Fi сети!

---

## 🎓 Установка Node.js на Windows

Если Node.js не установлен:

1. Скачайте с https://nodejs.org/
2. Выберите LTS версию (рекомендуется)
3. Запустите установщик
4. Следуйте инструкциям
5. Перезапустите терминал
6. Проверьте: `node --version`

---

## 🚀 Готовы начать?

```cmd
npm install
npm run server
npm run dev
```

Откройте: http://localhost:3000 или http://127.0.0.1:3000

**Готово!** 🎉

---

## 📞 Всё ещё не работает?

1. Перезагрузите компьютер
2. Переустановите Node.js
3. Проверьте антивирус
4. Проверьте firewall
5. Попробуйте другой браузер
6. Смотрите [ALTERNATIVE_PORTS.md](ALTERNATIVE_PORTS.md)

---

**Версия для Windows 10/11**  
**Протестировано:** ✅
