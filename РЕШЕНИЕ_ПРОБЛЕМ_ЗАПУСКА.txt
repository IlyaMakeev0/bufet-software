╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║              🔧 РЕШЕНИЕ ПРОБЛЕМ ПРИ ЗАПУСКЕ                  ║
║              Быстрые решения типичных ошибок                  ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 1: "Страница недоступна" / ERR_EMPTY_RESPONSE
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Backend не запустился или упал

РЕШЕНИЕ:
1. Посмотрите логи:
   docker-compose -f docker-compose.production.yml logs backend

2. Если видите ошибку подключения к БД:
   docker-compose -f docker-compose.production.yml restart postgres
   timeout /t 10 /nobreak
   docker-compose -f docker-compose.production.yml restart backend

3. Если не помогло - полная перезагрузка:
   docker-compose -f docker-compose.production.yml down
   docker-compose -f docker-compose.production.yml up -d --build


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 2: "Port is already allocated" (порт занят)
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Порты 80 или 443 заняты другим процессом

РЕШЕНИЕ:
1. Проверьте, что занимает порты:
   netstat -ano | findstr ":80 "
   netstat -ano | findstr ":443 "

2. Остановите старые контейнеры:
   docker-compose -f docker-compose.production.yml down

3. Если порт 443 занят Caddy/hysteria2:
   net stop caddy
   или
   taskkill /F /IM caddy.exe

4. Запустите снова:
   docker-compose -f docker-compose.production.yml up -d


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 3: PostgreSQL не запускается (unhealthy)
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Проблемы с данными PostgreSQL

РЕШЕНИЕ:
1. Остановите все:
   docker-compose -f docker-compose.production.yml down

2. Удалите volume (ВНИМАНИЕ: удалит все данные БД):
   docker volume rm bufet-software_postgres_data

3. Запустите заново:
   docker-compose -f docker-compose.production.yml up -d postgres
   timeout /t 20 /nobreak
   docker exec -i school-canteen-db psql -U canteen_user -d school_canteen < add-menu-requests-and-notifications.sql
   docker-compose -f docker-compose.production.yml up -d


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 4: "relation menu_requests does not exist"
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Миграция БД не применена

РЕШЕНИЕ:
1. Убедитесь, что PostgreSQL запущен:
   docker-compose -f docker-compose.production.yml ps postgres

2. Примените миграцию:
   docker exec -i school-canteen-db psql -U canteen_user -d school_canteen < add-menu-requests-and-notifications.sql

3. Перезапустите backend:
   docker-compose -f docker-compose.production.yml restart backend


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 5: Backend постоянно перезапускается (restarting)
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Ошибка в коде или конфигурации

РЕШЕНИЕ:
1. Посмотрите полные логи:
   docker-compose -f docker-compose.production.yml logs --tail=100 backend

2. Проверьте, что PostgreSQL работает:
   docker-compose -f docker-compose.production.yml logs postgres

3. Проверьте переменные окружения в .env файле

4. Пересоберите контейнер:
   docker-compose -f docker-compose.production.yml up -d --build backend


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 6: SSL/HTTPS не работает
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Проблемы с сертификатами

РЕШЕНИЕ:
1. Проверьте, что сертификаты на месте:
   dir cert\

   Должны быть:
   - cert\key.txt
   - cert\www_autogreatfood_ru_2026_08_30.crt

2. Проверьте права доступа к папке cert

3. Перезапустите backend:
   docker-compose -f docker-compose.production.yml restart backend


═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА 7: "Cannot connect to Docker daemon"
═══════════════════════════════════════════════════════════════

ПРИЧИНА: Docker не запущен

РЕШЕНИЕ:
1. Запустите Docker Desktop

2. Подождите, пока Docker полностью запустится (1-2 минуты)

3. Проверьте:
   docker --version

4. Запустите проект снова


═══════════════════════════════════════════════════════════════
  УНИВЕРСАЛЬНОЕ РЕШЕНИЕ: ПОЛНАЯ ПЕРЕЗАГРУЗКА
═══════════════════════════════════════════════════════════════

Если ничего не помогает:

1. Остановите все контейнеры:
   docker-compose -f docker-compose.production.yml down -v

2. Удалите старые образы:
   docker system prune -a

3. Пересоберите и запустите:
   docker-compose -f docker-compose.production.yml up -d --build

4. Примените миграцию:
   timeout /t 20 /nobreak
   docker exec -i school-canteen-db psql -U canteen_user -d school_canteen < add-menu-requests-and-notifications.sql


═══════════════════════════════════════════════════════════════
  ДИАГНОСТИКА
═══════════════════════════════════════════════════════════════

Для полной диагностики запустите:
  check-project.bat

Это покажет:
  ✅ Статус Docker
  ✅ Статус контейнеров
  ✅ Занятые порты
  ✅ Логи PostgreSQL
  ✅ Логи Backend
  ✅ HTTP статус сайта


═══════════════════════════════════════════════════════════════
  ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ ОТЛАДКИ
═══════════════════════════════════════════════════════════════

Посмотреть все контейнеры:
  docker ps -a

Посмотреть логи конкретного контейнера:
  docker logs school-canteen-backend
  docker logs school-canteen-db

Зайти внутрь контейнера:
  docker exec -it school-canteen-backend sh
  docker exec -it school-canteen-db bash

Проверить подключение к PostgreSQL:
  docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "\dt"

Посмотреть переменные окружения backend:
  docker exec school-canteen-backend env

Проверить, слушает ли backend порты:
  docker exec school-canteen-backend netstat -tlnp


═══════════════════════════════════════════════════════════════
  ЕСЛИ НИЧЕГО НЕ ПОМОГЛО
═══════════════════════════════════════════════════════════════

1. Запустите check-project.bat

2. Скопируйте вывод команды:
   docker-compose -f docker-compose.production.yml logs backend

3. Скопируйте вывод команды:
   docker-compose -f docker-compose.production.yml logs postgres

4. Пришлите мне эти логи - я помогу разобраться!


═══════════════════════════════════════════════════════════════

💡 В 90% случаев помогает простая перезагрузка:
   docker-compose -f docker-compose.production.yml down
   docker-compose -f docker-compose.production.yml up -d --build

