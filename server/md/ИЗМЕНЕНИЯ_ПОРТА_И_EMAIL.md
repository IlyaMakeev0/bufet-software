# ✅ Изменения: Порт и Email

## 🔄 Что изменено

### 1. Порт изменен с 5000 на 80

**Было:** `http://localhost:5000`  
**Стало:** `http://localhost` (порт 80 - стандартный HTTP)

#### Измененные файлы:
- ✅ `server/index.js` - PORT = 80
- ✅ `server/index-docker.js` - PORT = 80
- ✅ `docker-compose.production.yml` - порт 80:80
- ✅ `CICD/docker-compose.production.yml` - порт 80:80
- ✅ `CICD/nginx.production.conf` - backend:80
- ✅ `.env.example` - PORT=80

### 2. Email сервис исправлен

**Проблема:** Email настройки были захардкожены в коде  
**Решение:** Теперь используются переменные окружения

#### Измененные файлы:
- ✅ `server/utils/emailService.js` - использует process.env
- ✅ `.env.example` - добавлены комментарии и альтернативы

### 3. Созданы документы с тестовыми данными

- ✅ `ТЕСТОВЫЕ_ДАННЫЕ.md` - Полная информация о тестовых аккаунтах
- ✅ `БЫСТРЫЙ_ВХОД.txt` - Краткая справка для быстрого входа
- ✅ `ИЗМЕНЕНИЯ_ПОРТА_И_EMAIL.md` - Этот файл

## 🔑 Тестовые аккаунты

### Готовые аккаунты (БЕЗ email верификации):

```
Ученик:         student@test.com  / test123
Ученик 2:       student2@test.com / test123
Ученик 3:       student3@test.com / test123
Повар:          chef@test.com     / test123
Администратор:  admin@test.com    / test123
```

### Быстрый вход:

```bash
# Откройте в браузере
http://localhost/student-auth
http://localhost/chef-auth
http://localhost/admin-auth

# Введите тестовые данные
Email: student@test.com
Пароль: test123
```

## 📧 Настройка Email (опционально)

### Почему не приходят коды?

Email сервис требует настройки SMTP. Для тестирования используйте готовые аккаунты!

### Если нужно настроить email:

#### Вариант 1: Gmail (рекомендуется)

1. **Включите 2FA:**
   - https://myaccount.google.com/security

2. **Создайте пароль приложения:**
   - https://myaccount.google.com/apppasswords
   - Выберите "Почта" → "Другое устройство"
   - Скопируйте пароль (16 символов)

3. **Создайте .env файл:**
   ```bash
   cp .env.example .env
   ```

4. **Отредактируйте .env:**
   ```env
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USER=ваш-email@gmail.com
   EMAIL_PASSWORD=xxxx xxxx xxxx xxxx
   ```

5. **Перезапустите сервер:**
   ```bash
   npm run server
   ```

#### Вариант 2: Yandex Mail

```env
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USER=ваш-email@yandex.ru
EMAIL_PASSWORD=ваш-пароль
```

#### Вариант 3: Mail.ru

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=ваш-email@mail.ru
EMAIL_PASSWORD=ваш-пароль
```

#### Вариант 4: Mailtrap (для тестирования)

```env
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=ваш-mailtrap-user
EMAIL_PASSWORD=ваш-mailtrap-pass
```

## 🚀 Как запустить с новыми настройками

### Локально:

```bash
# 1. Остановите текущий сервер (если запущен)
Ctrl+C

# 2. Создайте .env (если еще не создан)
cp .env.example .env

# 3. Запустите сервер на порту 80
npm run server

# 4. Откройте в браузере
http://localhost
```

### С Docker:

```bash
# 1. Остановите контейнеры
docker-compose -f docker-compose.production.yml down

# 2. Пересоберите с новыми настройками
docker-compose -f docker-compose.production.yml up -d --build

# 3. Проверьте логи
docker-compose -f docker-compose.production.yml logs -f backend

# 4. Откройте в браузере
http://localhost
```

## ⚠️ Важные замечания

### Порт 80 на Windows

Порт 80 может быть занят системными службами (IIS, Skype, и т.д.)

**Если порт занят:**

1. **Проверьте что занимает порт:**
   ```powershell
   netstat -ano | findstr :80
   ```

2. **Измените порт в server/index.js:**
   ```javascript
   const PORT = process.env.PORT || 8080  // Или любой другой
   ```

3. **Перезапустите сервер**

### Альтернативные порты:

- **8080** - Часто используется для веб-приложений
- **3000** - Стандарт для Node.js приложений
- **8000** - Альтернатива
- **3001** - Если 3000 занят

### Email в production

**ВАЖНО:** Перед production деплоем:

1. ✅ Настройте реальный SMTP сервис
2. ✅ Используйте переменные окружения (.env)
3. ✅ НЕ коммитьте credentials в Git
4. ✅ Добавьте .env в .gitignore
5. ✅ Используйте пароли приложений (не основной пароль)

## 🧪 Тестирование

### Проверка порта:

```bash
# Проверьте что сервер запущен
curl http://localhost/api/menu

# Или откройте в браузере
http://localhost
```

### Проверка email:

```bash
# Проверьте логи сервера
# Должно быть:
# 📧 Email сервис настроен:
#    Host: smtp.gmail.com
#    Port: 587
#    User: ваш-email@gmail.com
#    Password: ***
```

### Проверка тестовых аккаунтов:

```bash
# Откройте базу данных
sqlite3 school_canteen.db

# Проверьте аккаунты
SELECT email, role, email_verified, balance 
FROM users 
WHERE email LIKE '%@test.com';

# Должно показать 5 аккаунтов
```

## 📚 Документация

### Основные документы:

- 👉 **БЫСТРЫЙ_ВХОД.txt** - Тестовые данные для входа
- 👉 **ТЕСТОВЫЕ_ДАННЫЕ.md** - Полная информация об аккаунтах и email
- 👉 **ИЗМЕНЕНИЯ_ПОРТА_И_EMAIL.md** - Этот файл

### Дополнительно:

- `README.md` - Общая информация о проекте
- `.env.example` - Пример переменных окружения
- `server/utils/emailService.js` - Email сервис

## ✅ Чек-лист

- [ ] Сервер запущен на порту 80
- [ ] Приложение открывается по http://localhost
- [ ] Тестовые аккаунты работают
- [ ] Можно войти без email верификации
- [ ] Email сервис настроен (опционально)
- [ ] .env файл создан и заполнен

## 🎯 Следующие шаги

1. ✅ Используйте тестовые аккаунты для разработки
2. ✅ Настройте email для production (когда понадобится)
3. ✅ Протестируйте все функции приложения
4. ✅ Перед production удалите тестовые аккаунты

## 📞 Поддержка

### Проблемы с портом:

- Порт 80 занят → Измените на 8080 или 3000
- Нет доступа к порту 80 → Запустите от администратора
- Firewall блокирует → Добавьте исключение

### Проблемы с email:

- Коды не приходят → Используйте тестовые аккаунты
- Нужна настройка → Смотрите ТЕСТОВЫЕ_ДАННЫЕ.md
- Ошибка SMTP → Проверьте credentials в .env

### Проблемы с входом:

- Аккаунт не найден → Примените create-test-accounts.sql
- Неверный пароль → Используйте test123
- Email не верифицирован → Тестовые аккаунты уже верифицированы

---

**Готово!** 🎉

Используйте `student@test.com` / `test123` для быстрого входа на http://localhost

Полная документация: **ТЕСТОВЫЕ_ДАННЫЕ.md**
