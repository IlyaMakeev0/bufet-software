# 🚀 Применение изменений системы склада

## Быстрый старт

### Вариант 1: Пересоздание базы данных (рекомендуется)

```bash
# Запустите скрипт
reset-database.bat
```

Этот скрипт:
1. Остановит все контейнеры
2. Удалит старую базу данных
3. Создаст новую с правильной структурой
4. Заполнит тестовыми данными

⚠️ **ВНИМАНИЕ:** Все текущие данные будут удалены!

### Вариант 2: Миграция существующей базы (для продакшена)

Если у вас есть важные данные, выполните SQL-миграцию:

```sql
-- 1. Создать таблицу menu_ingredients
CREATE TABLE menu_ingredients (
  id TEXT PRIMARY KEY,
  menu_id TEXT NOT NULL,
  ingredient_name TEXT NOT NULL,
  quantity DECIMAL(10, 2) NOT NULL,
  unit TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (menu_id) REFERENCES menu(id) ON DELETE CASCADE
);

-- 2. Создать таблицу inventory_log
CREATE TABLE inventory_log (
  id TEXT PRIMARY KEY,
  inventory_id TEXT NOT NULL,
  action TEXT NOT NULL,
  quantity_change DECIMAL(10, 2) NOT NULL,
  quantity_before DECIMAL(10, 2) NOT NULL,
  quantity_after DECIMAL(10, 2) NOT NULL,
  reason TEXT,
  created_by TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (inventory_id) REFERENCES inventory(id),
  FOREIGN KEY (created_by) REFERENCES users(id)
);

-- 3. Добавить поле min_quantity в inventory
ALTER TABLE inventory ADD COLUMN min_quantity DECIMAL(10, 2) DEFAULT 10;

-- 4. Обновить существующие записи
UPDATE inventory SET min_quantity = 10 WHERE min_quantity IS NULL;
```

## Проверка работы

### 1. Проверьте склад
1. Войдите как повар
2. Перейдите на вкладку "📦 Склад"
3. Должны быть видны ~30 продуктов с количеством
4. Попробуйте изменить количество - должна появиться запись в истории

### 2. Проверьте выдачу блюд
1. Создайте заказ как студент
2. Войдите как повар
3. Попробуйте выдать блюдо
4. Проверьте, что количество ингредиентов уменьшилось

### 3. Проверьте историю
1. Нажмите 📊 на любом продукте
2. Должна открыться история изменений
3. Проверьте, что списание отображается корректно

### 4. Проверьте заявки
1. Уменьшите количество продукта ниже минимума
2. Перейдите на вкладку "🛒 Заявки"
3. Должна появиться рекомендация заказать этот продукт

## Что изменилось в коде

### Backend (server/)
- `database.js` - добавлены новые таблицы и тестовые данные
- `routes/chef.js` - обновлена логика выдачи блюд, добавлены новые эндпоинты

### Frontend (src/)
- `components/ChefDashboard.jsx` - улучшен UI склада и заявок
- `pages/Dashboard.css` - добавлены новые стили

## Возможные проблемы

### Ошибка "Недостаточно ингредиентов"
**Причина:** На складе не хватает продуктов для блюда
**Решение:** Пополните склад или создайте заявку на закупку

### Не отображается история
**Причина:** Таблица inventory_log не создана
**Решение:** Выполните миграцию или пересоздайте БД

### Продукты не списываются
**Причина:** Нет связи между блюдами и ингредиентами
**Решение:** Проверьте таблицу menu_ingredients

## Тестовые данные

После пересоздания БД будут доступны:
- 30+ продуктов на складе
- Все блюда связаны с ингредиентами
- Минимальные остатки установлены для всех продуктов

## Следующие шаги

1. Протестируйте систему
2. Настройте минимальные остатки под ваши нужды
3. Создайте заявки на закупку для продуктов с низким запасом
4. Обучите поваров работе с новой системой

## Поддержка

Если что-то не работает:
1. Проверьте логи Docker: `docker-compose logs`
2. Проверьте структуру БД
3. Убедитесь, что все файлы обновлены
4. Попробуйте пересоздать БД

---

**Документация:** См. `md/INVENTORY_SYSTEM.md` для подробностей
