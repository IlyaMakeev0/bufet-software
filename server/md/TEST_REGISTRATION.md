# 🧪 Тестирование регистрации

## ✅ Приложение запущено!

### Проверка статуса:
```bash
docker ps
```

Должны быть запущены:
- `school-canteen-app` (порт 5000)
- `school-canteen-db` (порт 5432)

---

## 📝 Тест регистрации студента

### 1. Откройте страницу регистрации:
```
http://localhost:5000/student-auth
```

### 2. Заполните форму:
- **Email:** test@student.com
- **Пароль:** Test123!
- **Имя:** Тест
- **Фамилия:** Тестов
- **Телефон:** +79991234567
- **Класс:** 10А
- **Код подтверждения:** 1111

### 3. Нажмите "Зарегистрироваться"

---

## 🔍 Если возникает ошибка

### Просмотр логов в реальном времени:
```bash
docker-compose -f docker-compose.simple.yml logs app -f
```

### Проверка последних ошибок:
```bash
docker-compose -f docker-compose.simple.yml logs app --tail=50 | findstr /i "error"
```

### Проверка базы данных:
```bash
# Подключение к PostgreSQL
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen

# Внутри psql:
\dt                          # Список таблиц
SELECT * FROM users;         # Проверка пользователей
\q                           # Выход
```

---

## ✅ Что было исправлено

### 1. Убрана поддержка SQLite
- Удалена зависимость `sqlite3` из package.json
- `server/database.js` теперь использует только PostgreSQL
- `server/index.js` обновлен для работы с PostgreSQL

### 2. Исправлен SQL синтаксис
- `available = 1` → `available = true` (PostgreSQL boolean)
- Автоматическая конвертация `?` → `$1, $2, $3` в запросах

### 3. Добавлено ожидание БД
- `server/wait-for-db.js` - ждет готовности PostgreSQL
- До 30 попыток с интервалом 2 секунды

---

## 🎯 Ожидаемый результат

После успешной регистрации:
1. Вы будете перенаправлены на dashboard
2. Увидите баланс: 1000 ₽
3. Сможете просматривать меню
4. Сможете создавать заказы

---

## 📊 Проверка в базе данных

### После регистрации проверьте:
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "SELECT email, first_name, last_name, role, balance FROM users;"
```

Должен появиться новый пользователь:
```
      email       | first_name | last_name | role    | balance
------------------+------------+-----------+---------+---------
 test@student.com | Тест       | Тестов    | student | 1000.00
```

---

## 🔧 Troubleshooting

### Ошибка: "Пользователь уже существует"
```bash
# Удалите пользователя
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "DELETE FROM users WHERE email='test@student.com';"
```

### Ошибка: "No such table"
```bash
# Пересоздайте контейнеры
docker-compose -f docker-compose.simple.yml down -v
docker-compose -f docker-compose.simple.yml up --build -d
```

### Ошибка: "Connection refused"
```bash
# Проверьте статус контейнеров
docker ps

# Проверьте логи БД
docker-compose -f docker-compose.simple.yml logs db
```

---

## 🎉 Успех!

Если регистрация прошла успешно:
- ✅ PostgreSQL работает
- ✅ Приложение подключено к БД
- ✅ API endpoints работают
- ✅ Frontend взаимодействует с backend

**Следующие шаги:**
1. Протестируйте вход в систему
2. Просмотрите меню
3. Создайте заказ
4. Проверьте баланс

---

**Версия:** 1.0.2  
**Дата:** 2024-01-24  
**Статус:** ✅ SQLite удален, только PostgreSQL
