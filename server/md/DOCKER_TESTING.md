# 🧪 Docker Testing Guide

## Пошаговое тестирование Docker-контейнеризации

Этот документ содержит полный чеклист для проверки работоспособности Docker-архитектуры.

---

## 📋 Pre-flight Checklist

### 1. Проверка установки Docker
```bash
docker --version
docker-compose --version
```

**Ожидаемый результат:**
```
Docker version 24.x.x
Docker Compose version v2.x.x
```

### 2. Проверка запуска Docker
```bash
docker ps
```

**Ожидаемый результат:**
- Команда выполняется без ошибок
- Может показать пустой список (это нормально)

### 3. Проверка конфигурации
```bash
docker-compose config
```

**Ожидаемый результат:**
- Конфигурация валидна
- Нет ошибок синтаксиса

---

## 🚀 Тест 1: Первый запуск

### Шаг 1: Сборка образов
```bash
docker-compose build
```

**Что проверяем:**
- [ ] Сборка завершается без ошибок
- [ ] Создается образ `bufet-software-app`
- [ ] Скачивается образ `postgres:15-alpine`

**Время выполнения:** 2-5 минут (первый раз)

### Шаг 2: Запуск контейнеров
```bash
docker-compose up -d
```

**Что проверяем:**
- [ ] Контейнер `school-canteen-db` запускается
- [ ] Контейнер `school-canteen-app` запускается
- [ ] Нет ошибок в выводе

**Время выполнения:** 10-30 секунд

### Шаг 3: Проверка статуса
```bash
docker ps
```

**Ожидаемый результат:**
```
CONTAINER ID   IMAGE                    STATUS         PORTS
xxxxx          bufet-software-app       Up X seconds   0.0.0.0:5000->5000/tcp
xxxxx          postgres:15-alpine       Up X seconds   0.0.0.0:5432->5432/tcp
```

**Что проверяем:**
- [ ] Оба контейнера в статусе "Up"
- [ ] Порты правильно проброшены
- [ ] Нет постоянных перезапусков

---

## 🔍 Тест 2: Проверка логов

### Логи базы данных
```bash
docker-compose logs db
```

**Что искать:**
- [ ] `database system is ready to accept connections`
- [ ] Нет ошибок подключения
- [ ] Нет ошибок инициализации

### Логи приложения
```bash
docker-compose logs app
```

**Что искать:**
- [ ] `🔄 Initializing PostgreSQL database...`
- [ ] `✅ PostgreSQL database initialized successfully!`
- [ ] `🚀 Server running on:`
- [ ] `http://localhost:5000`
- [ ] Нет ошибок подключения к БД

---

## 🌐 Тест 3: HTTP Endpoints

### Health Check
```bash
curl http://localhost:5000/health
```

**Ожидаемый результат:**
```json
{"status":"ok","timestamp":"2024-01-24T..."}
```

**Что проверяем:**
- [ ] Статус код 200
- [ ] JSON ответ корректен
- [ ] Timestamp актуален

### Главная страница
```bash
curl -I http://localhost:5000
```

**Ожидаемый результат:**
```
HTTP/1.1 200 OK
Content-Type: text/html
```

**Что проверяем:**
- [ ] Статус код 200
- [ ] Content-Type правильный
- [ ] Страница загружается

---

## 🗄️ Тест 4: База данных

### Подключение к PostgreSQL
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen
```

**Что проверяем:**
- [ ] Подключение успешно
- [ ] Нет ошибок аутентификации

### Проверка таблиц
```sql
\dt
```

**Ожидаемый результат:**
```
 Schema |     Name      | Type  |    Owner     
--------+---------------+-------+--------------
 public | issued_meals  | table | canteen_user
 public | menu          | table | canteen_user
 public | orders        | table | canteen_user
 public | reviews       | table | canteen_user
 public | subscriptions | table | canteen_user
 public | users         | table | canteen_user
```

**Что проверяем:**
- [ ] Все 6 таблиц созданы
- [ ] Owner правильный (canteen_user)

### Проверка данных меню
```sql
SELECT COUNT(*) FROM menu;
```

**Ожидаемый результат:**
```
 count 
-------
   270
(1 row)
```

**Что проверяем:**
- [ ] В таблице menu есть данные
- [ ] Количество записей ~270 (30 дней × 9 блюд)

### Проверка структуры
```sql
\d users
```

**Что проверяем:**
- [ ] Все колонки присутствуют
- [ ] Типы данных правильные
- [ ] Constraints настроены

### Выход из psql
```sql
\q
```

---

## 🖥️ Тест 5: Веб-интерфейс

### Открыть в браузере
```
http://localhost:5000
```

### Главная страница
**Что проверяем:**
- [ ] Страница загружается
- [ ] Нет ошибок в консоли браузера
- [ ] CSS стили применяются
- [ ] Навигация работает

### Регистрация студента
1. Перейти на `/student-auth`
2. Заполнить форму регистрации:
   - Email: `test@student.com`
   - Пароль: `Test123!`
   - Имя: `Тест`
   - Фамилия: `Тестов`
   - Класс: `10А`

**Что проверяем:**
- [ ] Форма отправляется
- [ ] Регистрация успешна
- [ ] Перенаправление на dashboard
- [ ] Данные сохранены в БД

### Проверка в БД
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "SELECT email, first_name, last_name FROM users WHERE email='test@student.com';"
```

**Ожидаемый результат:**
```
      email       | first_name | last_name 
------------------+------------+-----------
 test@student.com | Тест       | Тестов
```

### Вход в систему
1. Выйти из системы
2. Войти с теми же credentials

**Что проверяем:**
- [ ] Вход успешен
- [ ] Session сохраняется
- [ ] Dashboard загружается

### Просмотр меню
1. Перейти в раздел меню
2. Выбрать дату

**Что проверяем:**
- [ ] Меню загружается
- [ ] Блюда отображаются
- [ ] Цены корректны
- [ ] Можно добавить в заказ

### Создание заказа
1. Добавить блюдо в заказ
2. Оформить заказ

**Что проверяем:**
- [ ] Заказ создается
- [ ] Баланс уменьшается
- [ ] Заказ отображается в истории

---

## 🔄 Тест 6: Перезапуск

### Остановка контейнеров
```bash
docker-compose stop
```

**Что проверяем:**
- [ ] Контейнеры останавливаются
- [ ] Нет ошибок

### Проверка статуса
```bash
docker ps -a
```

**Ожидаемый результат:**
- Контейнеры в статусе "Exited"

### Запуск контейнеров
```bash
docker-compose start
```

**Что проверяем:**
- [ ] Контейнеры запускаются
- [ ] Данные сохранились
- [ ] Приложение работает

### Проверка данных
```bash
docker exec -it school-canteen-db psql -U canteen_user -d school_canteen -c "SELECT COUNT(*) FROM users;"
```

**Что проверяем:**
- [ ] Данные не потеряны
- [ ] Пользователь test@student.com существует

---

## 🧹 Тест 7: Очистка и пересоздание

### Полная остановка
```bash
docker-compose down
```

**Что проверяем:**
- [ ] Контейнеры удалены
- [ ] Сеть удалена
- [ ] Volumes сохранены

### Проверка volumes
```bash
docker volume ls | grep bufet-software
```

**Ожидаемый результат:**
```
bufet-software_app_data
bufet-software_postgres_data
```

### Пересоздание
```bash
docker-compose up -d
```

**Что проверяем:**
- [ ] Контейнеры создаются заново
- [ ] Данные восстанавливаются из volumes
- [ ] Приложение работает
- [ ] Пользователь test@student.com существует

### Полная очистка (опционально)
```bash
docker-compose down -v
```

**Что проверяем:**
- [ ] Volumes удалены
- [ ] Все данные очищены

### Создание с нуля
```bash
docker-compose up --build -d
```

**Что проверяем:**
- [ ] Образы пересобираются
- [ ] БД инициализируется заново
- [ ] Меню создается (270 записей)
- [ ] Приложение работает

---

## 🔧 Тест 8: Troubleshooting

### Тест: Порт занят
1. Запустить локальный сервер на порту 5000
2. Попытаться запустить Docker

**Ожидаемая ошибка:**
```
Error: bind: address already in use
```

**Решение:**
- Остановить локальный сервер
- Или изменить порт в docker-compose.yml

### Тест: Контейнер падает
1. Намеренно сломать конфигурацию (неправильный DB_HOST)
2. Запустить контейнеры

**Что проверяем:**
- [ ] Логи показывают ошибку подключения
- [ ] Контейнер перезапускается
- [ ] После исправления работает

### Тест: Пересборка без кэша
```bash
docker-compose build --no-cache
docker-compose up -d
```

**Что проверяем:**
- [ ] Сборка проходит с нуля
- [ ] Все зависимости скачиваются заново
- [ ] Приложение работает

---

## 📊 Тест 9: Производительность

### Использование ресурсов
```bash
docker stats --no-stream
```

**Что проверяем:**
- [ ] CPU usage < 50%
- [ ] Memory usage < 512MB (app)
- [ ] Memory usage < 256MB (db)

### Время запуска
```bash
time docker-compose up -d
```

**Ожидаемое время:**
- Первый запуск: 2-5 минут
- Последующие: 10-30 секунд

### Время ответа API
```bash
time curl http://localhost:5000/health
```

**Ожидаемое время:**
- < 100ms

---

## 🌐 Тест 10: Сеть

### Проверка сети
```bash
docker network inspect bufet-software_canteen-network
```

**Что проверяем:**
- [ ] Оба контейнера в сети
- [ ] IP адреса назначены
- [ ] Subnet правильный

### Тест связи между контейнерами
```bash
docker exec school-canteen-app ping -c 3 db
```

**Ожидаемый результат:**
```
3 packets transmitted, 3 received, 0% packet loss
```

### Тест подключения к БД из приложения
```bash
docker exec school-canteen-app node -e "
const pg = require('pg');
const pool = new pg.Pool({
  host: 'db',
  port: 5432,
  database: 'school_canteen',
  user: 'canteen_user',
  password: 'canteen_password_2024'
});
pool.query('SELECT NOW()', (err, res) => {
  console.log(err ? err : res.rows[0]);
  pool.end();
});
"
```

**Что проверяем:**
- [ ] Подключение успешно
- [ ] Запрос выполняется
- [ ] Время возвращается

---

## ✅ Финальный чеклист

### Инфраструктура
- [ ] Docker установлен и запущен
- [ ] docker-compose работает
- [ ] Конфигурация валидна

### Контейнеры
- [ ] Образы собираются без ошибок
- [ ] Контейнеры запускаются
- [ ] Контейнеры работают стабильно
- [ ] Логи не содержат ошибок

### База данных
- [ ] PostgreSQL запускается
- [ ] Таблицы создаются
- [ ] Данные инициализируются
- [ ] Подключение работает
- [ ] Данные сохраняются между перезапусками

### Приложение
- [ ] Frontend собирается
- [ ] Backend запускается
- [ ] API endpoints работают
- [ ] Health check отвечает

### Функциональность
- [ ] Главная страница загружается
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Меню отображается
- [ ] Заказы создаются
- [ ] Данные сохраняются

### Производительность
- [ ] Время запуска приемлемое
- [ ] Использование ресурсов нормальное
- [ ] Время ответа API быстрое

### Надежность
- [ ] Перезапуск работает
- [ ] Данные не теряются
- [ ] Volumes работают
- [ ] Сеть стабильна

---

## 📝 Отчет о тестировании

### Шаблон отчета

```markdown
# Docker Testing Report

**Дата:** YYYY-MM-DD
**Тестировщик:** Имя
**Версия:** 1.0.0

## Результаты

### Pre-flight Checklist
- [ ] Docker установлен: ДА/НЕТ
- [ ] docker-compose работает: ДА/НЕТ
- [ ] Конфигурация валидна: ДА/НЕТ

### Тест 1: Первый запуск
- [ ] Сборка: УСПЕХ/ОШИБКА
- [ ] Запуск: УСПЕХ/ОШИБКА
- [ ] Статус: УСПЕХ/ОШИБКА

### Тест 2: Логи
- [ ] Логи БД: ОК/ОШИБКИ
- [ ] Логи приложения: ОК/ОШИБКИ

### Тест 3: HTTP Endpoints
- [ ] Health check: ОК/ОШИБКА
- [ ] Главная страница: ОК/ОШИБКА

### Тест 4: База данных
- [ ] Подключение: ОК/ОШИБКА
- [ ] Таблицы: ОК/ОШИБКА
- [ ] Данные: ОК/ОШИБКА

### Тест 5: Веб-интерфейс
- [ ] Загрузка: ОК/ОШИБКА
- [ ] Регистрация: ОК/ОШИБКА
- [ ] Вход: ОК/ОШИБКА
- [ ] Меню: ОК/ОШИБКА
- [ ] Заказы: ОК/ОШИБКА

### Тест 6: Перезапуск
- [ ] Остановка: ОК/ОШИБКА
- [ ] Запуск: ОК/ОШИБКА
- [ ] Сохранение данных: ОК/ОШИБКА

### Тест 7: Очистка
- [ ] Удаление: ОК/ОШИБКА
- [ ] Пересоздание: ОК/ОШИБКА

### Тест 8: Troubleshooting
- [ ] Обработка ошибок: ОК/ОШИБКА

### Тест 9: Производительность
- [ ] CPU: ОК/ВЫСОКАЯ
- [ ] Memory: ОК/ВЫСОКАЯ
- [ ] Время запуска: ОК/МЕДЛЕННО

### Тест 10: Сеть
- [ ] Связь между контейнерами: ОК/ОШИБКА

## Общий результат
- [ ] ВСЕ ТЕСТЫ ПРОЙДЕНЫ
- [ ] ЕСТЬ ПРОБЛЕМЫ (описать ниже)

## Проблемы
1. ...
2. ...

## Рекомендации
1. ...
2. ...
```

---

## 🎉 Заключение

Если все тесты пройдены успешно, Docker-контейнеризация работает корректно!

**Следующие шаги:**
1. Документировать любые найденные проблемы
2. Настроить CI/CD pipeline
3. Подготовить production конфигурацию
4. Настроить мониторинг и алертинг

**Вопросы?** См. DOCKER_GUIDE.md
