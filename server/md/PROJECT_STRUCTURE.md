# 📁 Структура проекта

## Обзор

Полный React проект школьной столовой, переписанный с Python Flask на современный стек React + Node.js.

## 🗂️ Файловая структура

```
school-canteen/
│
├── 📄 index.html                    # HTML точка входа
├── 📄 package.json                  # Зависимости и скрипты
├── 📄 vite.config.js                # Конфигурация Vite
├── 📄 .gitignore                    # Игнорируемые файлы
├── 📄 .env.example                  # Пример переменных окружения
├── 📄 README.md                     # Основная документация
├── 📄 QUICKSTART.md                 # Быстрый старт
├── 📄 PROJECT_STRUCTURE.md          # Этот файл
│
├── 📁 src/                          # Frontend исходники
│   ├── 📄 main.jsx                  # Точка входа React
│   ├── 📄 App.jsx                   # Главный компонент
│   ├── 📄 index.css                 # Глобальные стили
│   │
│   ├── 📁 pages/                    # Страницы приложения
│   │   ├── 📄 Home.jsx              # Главная страница
│   │   ├── 📄 Home.css              # Стили главной
│   │   ├── 📄 StudentAuth.jsx       # Авторизация ученика
│   │   ├── 📄 ChefAuth.jsx          # Авторизация повара
│   │   ├── 📄 AdminAuth.jsx         # Авторизация администратора
│   │   ├── 📄 Auth.css              # Общие стили авторизации
│   │   ├── 📄 Dashboard.jsx         # Главная панель
│   │   └── 📄 Dashboard.css         # Стили панели
│   │
│   └── 📁 components/               # React компоненты
│       ├── 📄 StudentDashboard.jsx  # Панель ученика
│       ├── 📄 ChefDashboard.jsx     # Панель повара
│       └── 📄 AdminDashboard.jsx    # Панель администратора
│
└── 📁 server/                       # Backend исходники
    ├── 📄 index.js                  # Главный файл сервера
    ├── 📄 database.js               # Работа с БД
    │
    ├── 📁 routes/                   # API маршруты
    │   ├── 📄 auth.js               # Аутентификация
    │   ├── 📄 menu.js               # Меню
    │   ├── 📄 orders.js             # Заказы
    │   ├── 📄 subscriptions.js      # Абонементы
    │   ├── 📄 chef.js               # API повара
    │   └── 📄 admin.js              # API администратора
    │
    └── 📁 utils/                    # Утилиты
        └── 📄 phone.js              # Валидация телефона
```

## 📦 Основные файлы

### Frontend

#### `src/main.jsx`
- Точка входа React приложения
- Рендерит корневой компонент

#### `src/App.jsx`
- Главный компонент приложения
- Настройка роутинга
- Управление состоянием пользователя
- Проверка аутентификации

#### `src/pages/Home.jsx`
- Главная страница с выбором роли
- Три карточки: Ученик, Повар, Администратор

#### `src/pages/StudentAuth.jsx`
- Вход и регистрация ученика
- Форма с полями: email, пароль, имя, фамилия, телефон, класс

#### `src/pages/ChefAuth.jsx`
- Вход и регистрация повара
- Форма с полями: email, пароль, имя, фамилия, телефон, должность

#### `src/pages/AdminAuth.jsx`
- Вход и регистрация администратора
- Форма с полями: email, пароль, имя, фамилия, телефон, должность

#### `src/pages/Dashboard.jsx`
- Главная панель после входа
- Роутинг на соответствующую панель по роли

#### `src/components/StudentDashboard.jsx`
- Панель ученика
- Просмотр баланса
- Меню на сегодня
- История заказов
- Абонементы

#### `src/components/ChefDashboard.jsx`
- Панель повара
- Список блюд на выдачу
- Выдача блюд
- История выданных блюд

#### `src/components/AdminDashboard.jsx`
- Панель администратора
- Общая статистика
- Список пользователей
- Последние заказы

### Backend

#### `server/index.js`
- Главный файл сервера Express
- Настройка middleware
- Подключение роутов
- Инициализация БД

#### `server/database.js`
- Подключение к SQLite
- Инициализация таблиц
- Генерация тестовых данных
- Утилиты для запросов

#### `server/routes/auth.js`
- POST `/api/auth/register` - регистрация
- POST `/api/auth/login` - вход
- POST `/api/auth/logout` - выход
- GET `/api/auth/check` - проверка сессии

#### `server/routes/menu.js`
- GET `/api/menu` - получить меню
- Фильтрация по дате

#### `server/routes/orders.js`
- GET `/api/orders` - получить заказы пользователя
- POST `/api/orders` - создать заказ
- Автоматическое списание с баланса

#### `server/routes/subscriptions.js`
- GET `/api/subscriptions` - получить абонементы
- POST `/api/subscriptions` - создать абонемент
- Расчет общей стоимости

#### `server/routes/chef.js`
- GET `/api/chef/pending-meals` - блюда на выдачу
- GET `/api/chef/issued-today` - выдано сегодня
- POST `/api/chef/issue-meal` - выдать блюдо

#### `server/routes/admin.js`
- GET `/api/admin/stats` - общая статистика
- GET `/api/admin/users` - все пользователи
- GET `/api/admin/recent-orders` - последние заказы

#### `server/utils/phone.js`
- `validatePhone()` - валидация номера телефона
- `formatPhone()` - форматирование в единый формат

## 🗄️ База данных

### Таблицы

#### `users`
- id (TEXT, PRIMARY KEY)
- email (TEXT, UNIQUE)
- password (TEXT, хешированный)
- first_name (TEXT)
- last_name (TEXT)
- phone (TEXT)
- class_name (TEXT, для учеников)
- position (TEXT, для сотрудников)
- role (TEXT: student/chef/admin)
- balance (REAL, для учеников)
- created_at (TIMESTAMP)

#### `menu`
- id (TEXT, PRIMARY KEY)
- day (TEXT, дата)
- name (TEXT, название блюда)
- description (TEXT)
- price (REAL)
- meal_type (TEXT: завтрак/обед/полдник)
- available (BOOLEAN)
- created_at (TIMESTAMP)

#### `orders`
- id (TEXT, PRIMARY KEY)
- user_id (TEXT, FK)
- menu_id (TEXT, FK)
- status (TEXT)
- created_at (TIMESTAMP)

#### `subscriptions`
- id (TEXT, PRIMARY KEY)
- user_id (TEXT, FK)
- menu_id (TEXT, FK)
- selected_dates (TEXT, JSON)
- start_date (TEXT)
- end_date (TEXT)
- total_price (REAL)
- status (TEXT)
- created_at (TIMESTAMP)

#### `issued_meals`
- id (TEXT, PRIMARY KEY)
- user_id (TEXT, FK)
- menu_id (TEXT, FK)
- subscription_id (TEXT, FK, nullable)
- issue_date (TEXT)
- meal_type (TEXT)
- issued_by (TEXT, FK, ID повара)
- status (TEXT)
- created_at (TIMESTAMP)

#### `reviews`
- id (TEXT, PRIMARY KEY)
- user_id (TEXT, FK)
- menu_id (TEXT, FK)
- rating (INTEGER, 1-5)
- comment (TEXT)
- created_at (TIMESTAMP)

## 🎨 Стили

### Глобальные (`src/index.css`)
- Сброс стилей
- Градиентный фон
- Общие классы кнопок
- Формы и инпуты
- Карточки

### Страницы
- `Home.css` - стили главной страницы
- `Auth.css` - стили авторизации
- `Dashboard.css` - стили панелей управления

## 🔧 Конфигурация

### `package.json`
- Зависимости проекта
- Скрипты запуска
- Метаданные

### `vite.config.js`
- Настройка Vite
- Прокси для API
- Порт разработки

### `.env.example`
- Пример переменных окружения
- Порт сервера
- Секретный ключ сессии

## 📝 Документация

### `README.md`
- Полное описание проекта
- Технологии
- Установка и запуск
- API endpoints
- Особенности

### `QUICKSTART.md`
- Быстрый старт
- Пошаговая инструкция
- Тестовые данные
- Устранение неполадок

### `PROJECT_STRUCTURE.md`
- Структура проекта (этот файл)
- Описание всех файлов
- Схема базы данных

## 🚀 Скрипты

```json
{
  "dev": "vite",              // Запуск клиента в режиме разработки
  "build": "vite build",      // Сборка для продакшена
  "preview": "vite preview",  // Предпросмотр сборки
  "server": "node server/index.js"  // Запуск сервера
}
```

## 🔐 Безопасность

- Пароли хешируются с bcryptjs
- Сессии с httpOnly cookies
- Валидация на клиенте и сервере
- Проверка прав доступа
- Защита от SQL-инъекций

## 📊 Потоки данных

### Регистрация
1. Пользователь заполняет форму
2. Клиент отправляет POST `/api/auth/register`
3. Сервер валидирует данные
4. Хеширует пароль
5. Создает пользователя в БД
6. Создает сессию
7. Возвращает данные пользователя

### Создание заказа
1. Ученик выбирает блюдо
2. Клиент отправляет POST `/api/orders`
3. Сервер проверяет баланс
4. Создает заказ
5. Списывает средства
6. Создает запись в issued_meals
7. Возвращает новый баланс

### Выдача блюда
1. Повар видит список на выдачу
2. Нажимает "Выдать"
3. Клиент отправляет POST `/api/chef/issue-meal`
4. Сервер обновляет статус
5. Записывает ID повара
6. Возвращает успех

## 🎯 Ключевые особенности

- **Модульная архитектура** - разделение на компоненты
- **RESTful API** - стандартные HTTP методы
- **Сессии** - безопасное хранение состояния
- **Валидация** - на клиенте и сервере
- **Адаптивность** - работает на всех устройствах
- **Масштабируемость** - легко добавлять функции

## 📈 Возможности расширения

1. **Добавить отзывы** - таблица reviews уже есть
2. **Уведомления** - WebSocket для real-time
3. **Оплата онлайн** - интеграция платежных систем
4. **Мобильное приложение** - React Native
5. **Аналитика** - графики и отчеты
6. **Экспорт данных** - CSV, Excel
7. **Фото блюд** - загрузка изображений
8. **Рейтинг блюд** - на основе отзывов

## 🔄 Миграция с Python

Проект полностью переписан с Flask на React + Node.js:

| Python Flask | React + Node.js |
|--------------|-----------------|
| Jinja2 templates | React components |
| Flask routes | Express routes |
| SQLite3 (Python) | SQLite3 (Node.js) |
| werkzeug.security | bcryptjs |
| Flask sessions | express-session |
| HTML в строках | JSX компоненты |

Все функции сохранены и улучшены!
