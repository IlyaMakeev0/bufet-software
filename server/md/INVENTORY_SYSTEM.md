# 📦 Система управления складом и ингредиентами

## Что было исправлено

### 1. Связь ингредиентов с блюдами
Теперь каждое блюдо в меню имеет список ингредиентов с указанием количества. При выдаче блюда ингредиенты автоматически списываются со склада.

**Новые таблицы в базе данных:**
- `menu_ingredients` - связь блюд с ингредиентами
- `inventory_log` - история всех изменений на складе

### 2. Автоматическое списание при выдаче
Когда повар выдает блюдо:
1. Система проверяет наличие всех ингредиентов
2. Если чего-то не хватает - выдача блокируется с подробным сообщением
3. При успешной выдаче все ингредиенты списываются автоматически
4. Все изменения логируются в историю

### 3. Улучшенный дизайн склада

#### Новые возможности:
- **Поиск продуктов** - быстрый поиск по названию
- **Минимальные остатки** - для каждого продукта можно установить минимум
- **Визуальные индикаторы** - прогресс-бары показывают уровень запасов
- **Цветовая индикация**:
  - 🔴 Красный - критически мало (ниже минимума)
  - 🟠 Оранжевый - требует внимания (150% от минимума)
  - 🟢 Зеленый - достаточно запасов

#### История изменений:
- Кнопка 📊 на каждой карточке продукта
- Показывает все операции: пополнение, списание
- Кто и когда изменил количество
- Причина изменения

### 4. Улучшенные заявки на закупку

#### Умные рекомендации:
- Система автоматически показывает продукты, которые заканчиваются
- Быстрое создание заявки одним кликом
- Автоматический расчет количества (минимум × 2)
- Автоматическая установка срочности

#### Визуальные улучшения:
- 🔴 Срочные заявки выделены красным
- 🟠 Высокий приоритет - оранжевым
- 🟢 Обычные - зеленым
- Анимация для срочных заявок

### 5. Статистика на главной

Новая карточка "Мало на складе":
- Показывает количество продуктов с низким запасом
- Анимация при наличии проблем
- Клик переводит на вкладку склада

## Структура данных

### Таблица menu_ingredients
```sql
CREATE TABLE menu_ingredients (
  id TEXT PRIMARY KEY,
  menu_id TEXT NOT NULL,           -- ID блюда
  ingredient_name TEXT NOT NULL,   -- Название ингредиента
  quantity DECIMAL(10, 2) NOT NULL, -- Количество на порцию
  unit TEXT NOT NULL,              -- Единица измерения
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (menu_id) REFERENCES menu(id) ON DELETE CASCADE
)
```

### Таблица inventory_log
```sql
CREATE TABLE inventory_log (
  id TEXT PRIMARY KEY,
  inventory_id TEXT NOT NULL,
  action TEXT NOT NULL,            -- 'списание' или 'пополнение'
  quantity_change DECIMAL(10, 2),  -- Изменение (+ или -)
  quantity_before DECIMAL(10, 2),  -- Было
  quantity_after DECIMAL(10, 2),   -- Стало
  reason TEXT,                     -- Причина
  created_by TEXT,                 -- Кто изменил
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

### Обновленная таблица inventory
```sql
CREATE TABLE inventory (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  quantity DECIMAL(10, 2) NOT NULL DEFAULT 0,
  unit TEXT NOT NULL,
  min_quantity DECIMAL(10, 2) DEFAULT 10,  -- НОВОЕ: минимальный остаток
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## Примеры использования

### Добавление блюда с ингредиентами
```javascript
// 1. Создаем блюдо
const menuId = await createMenuItem({
  name: 'Борщ',
  price: 150,
  meal_type: 'обед'
})

// 2. Добавляем ингредиенты
await addIngredient(menuId, 'Свекла', 0.15, 'кг')
await addIngredient(menuId, 'Капуста', 0.1, 'кг')
await addIngredient(menuId, 'Картофель', 0.12, 'кг')
```

### Выдача блюда
```javascript
// Система автоматически:
// 1. Проверит наличие всех ингредиентов
// 2. Спишет их со склада
// 3. Запишет в историю
// 4. Обновит статус блюда

await issueMeal(mealId)
```

### Просмотр истории продукта
```javascript
// Получить последние 20 операций
const log = await getInventoryLog(inventoryId, 20)

// Результат:
[
  {
    action: 'списание',
    quantityChange: -0.15,
    quantityBefore: 50,
    quantityAfter: 49.85,
    reason: 'Выдача блюда (issued_meal: xxx)',
    createdBy: 'Иван Петров',
    createdAt: '2025-01-24 14:30:00'
  }
]
```

## API эндпоинты

### Новые эндпоинты для повара

```
GET  /api/chef/menu/:menuId/ingredients
     Получить ингредиенты блюда

GET  /api/chef/inventory-log?inventoryId=xxx&limit=50
     Получить историю изменений склада

GET  /api/chef/inventory/low-stock
     Получить список продуктов с низким запасом
```

## Тестирование

1. **Создайте новое блюдо** через админ-панель
2. **Добавьте ингредиенты** (пока вручную через БД)
3. **Попробуйте выдать блюдо** - ингредиенты спишутся
4. **Проверьте историю** - нажмите 📊 на карточке продукта
5. **Создайте заявку** на закупку для продуктов с низким запасом

## Будущие улучшения

- [ ] UI для добавления ингредиентов к блюдам
- [ ] Автоматические заявки при достижении минимума
- [ ] Прогнозирование потребности в продуктах
- [ ] Экспорт истории в Excel
- [ ] Уведомления о критических остатках
- [ ] Интеграция с поставщиками

## Технические детали

### Проверка наличия ингредиентов
```javascript
// Перед выдачей блюда
const ingredients = await getMenuIngredients(menuId)
for (const ingredient of ingredients) {
  const stock = await getInventoryByName(ingredient.name)
  if (stock.quantity < ingredient.quantity) {
    throw new Error(`Недостаточно ${ingredient.name}`)
  }
}
```

### Списание ингредиентов
```javascript
// При выдаче блюда
for (const ingredient of ingredients) {
  await updateInventory(
    ingredient.name,
    -ingredient.quantity,  // Отрицательное значение = списание
    `Выдача блюда (${mealId})`
  )
}
```

## Поддержка

При возникновении проблем проверьте:
1. Все ли ингредиенты блюда есть на складе
2. Правильно ли указаны единицы измерения
3. Достаточно ли количества для выдачи

Логи можно посмотреть в истории склада (кнопка 📊).
