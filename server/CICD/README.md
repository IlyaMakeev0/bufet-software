# 🚀 CI/CD для проекта "Школьная столовая"

Полная система непрерывной интеграции и развертывания для приложения школьной столовой.

## 📁 Структура файлов

```
CICD/
├── README.md                      ← Вы здесь
├── БЫСТРЫЙ_СТАРТ.txt             ← Начните отсюда!
├── ПОЛНАЯ_ИНСТРУКЦИЯ.md          ← Детальное руководство
│
├── Dockerfile.backend             ← Backend Docker образ
├── Dockerfile.frontend            ← Frontend Docker образ
├── docker-compose.production.yml  ← Orchestration
│
├── nginx.production.conf          ← Nginx конфигурация
├── .dockerignore                  ← Исключения для Docker
├── .env.example                   ← Пример переменных окружения
│
├── .github-workflows-ci-cd.yml    ← GitHub Actions workflow
└── deploy.sh                      ← Скрипт деплоя
```

## 🎯 Что включено

### 1. Docker контейнеризация
- ✅ **Backend** - Node.js + Express сервер
- ✅ **Frontend** - React + Vite приложение
- ✅ **Database** - PostgreSQL 15
- ✅ **Nginx** - Reverse proxy и статические файлы

### 2. CI/CD Pipeline (GitHub Actions)
- ✅ Автоматическое тестирование
- ✅ Сборка приложения
- ✅ Создание Docker образов
- ✅ Публикация в GitHub Container Registry
- ✅ Автоматический деплой на сервер
- ✅ Health checks после деплоя

### 3. Production готовность
- ✅ Multi-stage Docker builds (оптимизация размера)
- ✅ Health checks для всех сервисов
- ✅ Gzip compression
- ✅ Security headers
- ✅ Кэширование статических файлов
- ✅ Логирование
- ✅ Graceful shutdown

## 🚀 Быстрый старт

### 1. Подготовка

```bash
# Переместите файлы
mkdir -p .github/workflows
mv CICD/.github-workflows-ci-cd.yml .github/workflows/ci-cd.yml
cp CICD/.dockerignore ./.dockerignore
cp CICD/docker-compose.production.yml ./

# Создайте .env
cp CICD/.env.example .env
# Отредактируйте .env
```

### 2. Локальное тестирование

```bash
# Запустите все сервисы
docker-compose -f docker-compose.production.yml up -d

# Проверьте статус
docker-compose -f docker-compose.production.yml ps

# Откройте в браузере
open http://localhost
```

### 3. Настройка GitHub

1. Создайте репозиторий на GitHub
2. Добавьте Secrets (Settings → Secrets and variables → Actions)
3. Запушьте код

```bash
git init
git add .
git commit -m "Initial commit with CI/CD"
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

### 4. Автоматический деплой

GitHub Actions автоматически:
- Запустит тесты
- Соберет приложение
- Создаст Docker образы
- Задеплоит на сервер

## 📖 Документация

### Для начинающих
👉 **[БЫСТРЫЙ_СТАРТ.txt](БЫСТРЫЙ_СТАРТ.txt)** - Пошаговая инструкция

### Для опытных
👉 **[ПОЛНАЯ_ИНСТРУКЦИЯ.md](ПОЛНАЯ_ИНСТРУКЦИЯ.md)** - Детальное руководство

## 🔐 Необходимые GitHub Secrets

### Для SSH деплоя:
```
SSH_HOST          = IP адрес сервера
SSH_USER          = пользователь SSH
SSH_PRIVATE_KEY   = приватный SSH ключ
SSH_PORT          = 22
```

### Для приложения:
```
DB_USER               = canteen_user
DB_PASSWORD           = ваш_пароль
SESSION_SECRET        = случайная_строка_32_символа
JWT_SECRET            = случайная_строка_32_символа
JWT_REFRESH_SECRET    = случайная_строка_32_символа
ENCRYPTION_KEY        = строка_ровно_32_символа
EMAIL_HOST            = smtp.gmail.com
EMAIL_PORT            = 587
EMAIL_USER            = ваш-email@gmail.com
EMAIL_PASSWORD        = пароль_приложения
FRONTEND_URL          = https://ваш-домен.com
BACKEND_URL           = https://ваш-домен.com
```

### Генерация секретов:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## 🛠️ Скрипт деплоя

Используйте `deploy.sh` для управления сервисами:

```bash
# Сделайте скрипт исполняемым
chmod +x CICD/deploy.sh

# Запустить сервисы
./CICD/deploy.sh start

# Остановить сервисы
./CICD/deploy.sh stop

# Показать статус
./CICD/deploy.sh status

# Показать логи
./CICD/deploy.sh logs

# Обновить приложение
./CICD/deploy.sh update

# Создать бэкап БД
./CICD/deploy.sh backup

# Проверить здоровье
./CICD/deploy.sh health

# Помощь
./CICD/deploy.sh help
```

## 📊 Архитектура

```
┌─────────────────────────────────────────┐
│         GitHub Repository               │
│  ┌──────────────────────────────────┐  │
│  │  Push → GitHub Actions Trigger   │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│        GitHub Actions CI/CD             │
│  ┌────────┐  ┌────────┐  ┌────────┐   │
│  │  Test  │→ │ Build  │→ │ Docker │   │
│  └────────┘  └────────┘  └────────┘   │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│    GitHub Container Registry            │
│  ┌──────────┐  ┌──────────┐            │
│  │ Backend  │  │ Frontend │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│      Production Server (VPS)            │
│  ┌──────────────────────────────────┐  │
│  │      Docker Compose              │  │
│  │  ┌────────┐ ┌────────┐ ┌──────┐ │  │
│  │  │Postgres│ │Backend │ │Nginx │ │  │
│  │  │ :5432  │ │ :5000  │ │ :80  │ │  │
│  │  └────────┘ └────────┘ └──────┘ │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 🔄 Workflow процесс

### При Push в main:
1. ✅ Запускаются тесты и линтинг
2. 🏗️ Собирается backend и frontend
3. 🐳 Создаются Docker образы
4. 📤 Образы публикуются в GHCR
5. 🚀 Автоматический деплой на сервер
6. 🏥 Health checks

### При Pull Request:
1. ✅ Запускаются тесты
2. 🏗️ Проверяется сборка
3. 📊 Отчет о результатах

## 📈 Мониторинг

### На сервере:

```bash
# Статус контейнеров
docker compose -f docker-compose.production.yml ps

# Логи
docker compose -f docker-compose.production.yml logs -f

# Использование ресурсов
docker stats

# Health checks
curl http://localhost:5000/api/menu
curl http://localhost/
```

### В GitHub:

- Откройте вкладку **Actions**
- Проверьте статус последних запусков
- Просмотрите логи каждого job

## 🐛 Troubleshooting

### Backend не запускается

```bash
docker compose -f docker-compose.production.yml restart postgres
sleep 10
docker compose -f docker-compose.production.yml restart backend
```

### Frontend показывает 502

```bash
docker compose -f docker-compose.production.yml restart frontend
```

### База данных не инициализируется

```bash
docker compose -f docker-compose.production.yml down -v
docker compose -f docker-compose.production.yml up -d
```

### Проверка логов

```bash
docker compose -f docker-compose.production.yml logs backend
docker compose -f docker-compose.production.yml logs frontend
docker compose -f docker-compose.production.yml logs postgres
```

## 📚 Полезные команды

### Docker Compose

```bash
# Запуск
docker compose -f docker-compose.production.yml up -d

# Остановка
docker compose -f docker-compose.production.yml down

# Перезапуск
docker compose -f docker-compose.production.yml restart

# Пересборка
docker compose -f docker-compose.production.yml build --no-cache

# Обновление
docker compose -f docker-compose.production.yml pull
docker compose -f docker-compose.production.yml up -d
```

### Docker

```bash
# Войти в контейнер
docker exec -it school-canteen-backend sh
docker exec -it school-canteen-db psql -U canteen_user

# Бэкап БД
docker exec school-canteen-db pg_dump -U canteen_user school_canteen > backup.sql

# Восстановление БД
docker exec -i school-canteen-db psql -U canteen_user school_canteen < backup.sql

# Очистка
docker system prune -af
docker volume prune -f
```

## 🔒 Безопасность

### Реализовано:
- ✅ Non-root пользователи в контейнерах
- ✅ Security headers в Nginx
- ✅ Secrets через переменные окружения
- ✅ Firewall настройки
- ✅ SSL/TLS поддержка (опционально)
- ✅ Health checks
- ✅ Логирование

### Рекомендации:
- 🔐 Используйте сильные пароли
- 🔐 Настройте SSL сертификаты
- 🔐 Регулярно обновляйте зависимости
- 🔐 Делайте бэкапы базы данных
- 🔐 Мониторьте логи

## 📊 Производительность

### Оптимизации:
- ✅ Multi-stage Docker builds
- ✅ Кэширование слоев Docker
- ✅ Gzip compression
- ✅ Кэширование статических файлов
- ✅ Connection pooling для БД
- ✅ Nginx buffering

### Рекомендуемые ресурсы сервера:
- **RAM**: 2GB минимум, 4GB рекомендуется
- **CPU**: 2 ядра
- **Диск**: 20GB свободного места
- **Сеть**: Стабильное подключение

## 🎯 Следующие шаги

1. ✅ Прочитайте [БЫСТРЫЙ_СТАРТ.txt](БЫСТРЫЙ_СТАРТ.txt)
2. ✅ Настройте GitHub Secrets
3. ✅ Протестируйте локально
4. ✅ Подготовьте сервер
5. ✅ Выполните первый деплой
6. ✅ Настройте мониторинг
7. ✅ Настройте SSL (опционально)
8. ✅ Настройте бэкапы

## 📞 Поддержка

При возникновении проблем:
1. Проверьте [ПОЛНАЯ_ИНСТРУКЦИЯ.md](ПОЛНАЯ_ИНСТРУКЦИЯ.md)
2. Проверьте логи сервисов
3. Проверьте GitHub Actions логи
4. Проверьте статус контейнеров

## ✅ Чек-лист готовности

- [ ] Файлы перемещены в правильные места
- [ ] GitHub репозиторий создан
- [ ] GitHub Secrets настроены
- [ ] Локальное тестирование пройдено
- [ ] Сервер подготовлен
- [ ] Docker установлен на сервере
- [ ] Firewall настроен
- [ ] SSH ключи настроены
- [ ] Первый деплой выполнен
- [ ] Health checks проходят
- [ ] Мониторинг настроен
- [ ] Бэкапы настроены

---

**Готово! Ваша CI/CD система настроена! 🎉**

Версия: 1.0.0  
Дата: 28 января 2026  
Проект: Школьная столовая
